# Ironfield RTS 战场升级执行计划

状态：方案 B 进行中（阶段 1–5 已完成，下一步为阶段 6）
目标版本：Combat Vertical Slice 0.2
基线版本：当前可玩的 9v9 原型

## 要做成什么

这一轮把当前的功能原型升级成一场可操控、可读、规模足够大的中世纪战斗。目标画面是 40v40，玩家仍然能框选和下令，同时能清楚看到前排接战、远程后排、法术覆盖、单位死亡和败军撤退。

参考站提供视觉方向，但不照搬市场数据、行情界面和自动演示逻辑。我们要保留 RTS 的玩家控制权，让玩家打乱初始阵型后，系统仍能维持清楚的战线和战斗反馈。

## 成功标准

- 默认战场为 40v40，共 80 个可见单位。
- 双方各有 2 支骑士小队、2 支游侠小队和 1 支法师小队，初始阵型有前、中、后三层。
- 地图包含至少 3 层高度、2 处水域、1 个中央争夺区、2 个营地或传送门，以及明确的通行和阻挡区域。
- 玩家可以框选、追加选择、移动、集火、攻击移动、停止和坚守。
- 骑士形成前线，游侠和法师保持职业射程，不再全部挤到同一点。
- 远程攻击有飞行过程，命中位置、伤害对象和画面特效来自同一条战斗事件。
- 单位有受击、死亡和撤退反馈，败方会向本方营地撤离。
- 默认 AI 对战在 60 至 100 秒内结束，前 10 秒不会直接混成一个单位团。
- 当前开发机器以 1280×720 运行 40v40 时，10 秒采样的中位帧率不低于 55 FPS，1% low 不低于 40 FPS。
- `pnpm check` 全部通过，实际浏览器控制台没有 warning 或 error。

## 本轮不做

- 市场行情、交易所连接和价格驱动战斗。
- 资源采集、建筑、科技树、人口和生产队列。
- 多人联机、账号、存档和后端服务。
- 随机生成无限地图。本轮只做一张经过设计的固定岛屿战场。
- 完整英雄系统、主动技能和装备系统。
- 移动端操作。布局可以正常缩放，但验收以桌面端为准。

## 方案选择

### 方案 A：只做视觉加法

保留 9v9 规则，增加地形、粒子和音效。改动小，能很快变好看，但单位数量、阵型和混战方式不变，最后仍像小规模遭遇战。

### 方案 B：40v40 垂直切片

重做地图、镜头、小队移动、交战编排和战斗事件，再补特效与声音。它能解决现在最明显的空、散、挤和反馈弱，同时不把范围扩大到完整 RTS。

### 方案 C：直接做完整 RTS

在方案 B 之上继续加入经济、建筑、生产和完整寻路。架构更完整，开发周期会被大量非战斗系统占用，无法尽快验证战场表现。

本轮采用方案 B。它会改动十个以上文件，并拆开目前过大的 `App.tsx` 和 `BattlefieldCanvas.tsx`，但不增加新的运行时语言、后端或外部服务。

## 系统边界

战斗模拟继续保持纯 TypeScript，不读取 Three.js 对象。渲染层只消费模拟状态和战斗事件，不能自行猜测命中目标、伤害时机或撤退状态。

```text
鼠标和键盘输入
      |
      v
命令层：选择、移动、攻击移动、集火、停止、坚守
      |
      v
小队层：阵型、朝向、槽位分配、局部避让
      |
      v
战斗模拟：目标、射程、投射物、伤害、死亡、撤退、胜负
      |
      +----------------------+
      |                      |
      v                      v
权威战斗事件              可序列化战斗状态
      |                      |
      +-----------+----------+
                  v
场景表现：角色动画、弹道、粒子、音效、镜头、HUD
```

## 代码调整方向

- `src/game/battle.ts` 保留战斗总循环和公开类型，逐步把职责拆到下面的模块。
- 新增 `src/game/squads.ts`，管理小队成员、阵型和撤退状态。
- 新增 `src/game/formation.ts`，计算阵型槽位、朝向和重排规则。
- 新增 `src/game/navigation.ts`，处理六边形地图通行、A* 路径和局部避让。
- 新增 `src/game/projectiles.ts`，处理箭矢和法术投射物的飞行与命中。
- 新增 `src/game/events.ts`，定义攻击、命中、死亡和撤退事件。
- 新增 `src/map/battlefield.ts`，保存固定地图的地块、高度、阻挡和营地数据。
- `src/scene/BattlefieldCanvas.tsx` 只负责组装场景，不继续堆入具体实现。
- 新增 `src/scene/terrain`、`src/scene/units`、`src/scene/effects` 和 `src/scene/camera`。
- 把 `src/App.tsx` 中的战斗 HUD 和输入协调拆到 `src/ui`，稳定保留当前可交互节点。

## 阶段 0：冻结基线和建立对照

依赖：无。

工作内容：

- [ ] 保存当前原型和参考站在同一视口下的初始、接战、结束三组截图。
- [ ] 记录当前 18 单位时的帧率、draw call、三角形数量和 JS 主线程耗时。
- [ ] 增加 `?benchmark=80` 开发入口，可以固定加载 80 个单位并显示渲染统计。
- [ ] 把现有 10 个测试作为回归基线，确认战斗结果可重复。

验收：

- 三组对照截图和性能基线写入 `docs/baseline.md`。
- `pnpm check` 保持通过。

## 阶段 1：建立小队与战斗事件骨架

依赖：阶段 0。

工作内容：

- [x] 为单位增加 `squadId`，为战斗状态增加小队集合和单调递增的事件序号。
- [x] 定义 `AttackStarted`、`ProjectileSpawned`、`ProjectileHit`、`DamageApplied`、`UnitDied` 和 `SquadRouted` 六类事件。
- [x] 把当前远程攻击改成投射物命中后结算伤害，近战攻击在攻击动作的命中点结算。
- [x] 事件只保留最近两秒，渲染消费事件时不反向修改模拟状态。
- [x] 保持固定步长和确定性，相同初始状态与命令必须得到相同事件序列。

测试：

- [x] 同一输入运行两次，状态和事件序列完全一致。
- [x] 投射物只伤害记录的目标，目标提前死亡后不能重复结算。
- [x] 同一单位死亡事件只出现一次。
- [x] 两秒以前的事件会被清理，不产生无限增长。

验收：

- 场景里的箭矢、法术和伤害数字都能追溯到同一条权威事件。
- 原有移动、集火、范围伤害和胜负测试继续通过。

## 阶段 2：重做岛屿地图与镜头

依赖：阶段 0。可以和阶段 1 分支开发，合并时以阶段 1 的状态类型为准。

工作内容：

- [x] 用六边形地块数据定义固定岛屿，地块包含坐标、高度、地表类型、通行状态和阵营区域。
- [x] 建立低地、主战场和高地三层高度，水域把岛屿切出两条自然边界，中央保留一条主通道和两条侧翼路线。
- [x] 使用现有 KayKit 水面、树木、岩石、帐篷和建筑资源布置营地与地标。
- [x] 调整地表颜色，降低当前草地的荧光感，增加雾、接触阴影和水面层次。
- [x] 镜头改为更低、更近的斜俯视构图。左键仍用于框选，右键仍用于下令，中键拖动旋转，方向键平移，滚轮缩放。
- [x] 镜头移动限制在岛屿边界内，重开战斗后恢复默认观察位置。

测试：

- [x] 地图数据没有重复坐标，所有出生点都位于可通行地块。
- [x] 双方出生区到中央主战场至少各有一条连通路径。
- [x] 水域、森林和岩石阻挡不会被路径搜索穿过。

验收：

- 1280×720 截图中，岛屿占主要画面，中央战区没有大面积无意义留白。
- 初始画面能直接读出双方营地、主通道、侧翼路线和中央争夺区。

## 阶段 3：扩展到 40v40，并保持阵型

依赖：阶段 1、阶段 2。

工作内容：

- [x] 每个阵营建立 5 支小队：两支 10 人骑士队、两支 8 人游侠队和一支 4 人法师队。
- [x] 阵型槽位保存稳定的成员目标，不按更新帧或单位 ID 重新洗牌。
- [x] 移动命令先计算小队目标，再分配成员槽位，仅在新命令时重排。
- [x] 使用六边形 A* 处理跨区域移动，使用局部避让处理单位之间的短距离冲突。
- [x] 非交战状态下，同阵营单位通过局部避让保持间距。
- [x] 新增攻击移动、停止和坚守命令，原有移动与指定集火继续保留。

测试：

- [x] 40 个单位获得 40 个不重复的阵型槽位。
- [x] 连续更新相同命令不会交换成员槽位或产生抖动。
- [x] 路线被水域阻断时，小队会选择可通行路径。
- [x] 单位保持稳定目标槽位，停止命令会立即清除移动意图。

验收：

- 开战前能清楚看到骑士、游侠和法师三层阵型。
- 大队移动时保持队形，不出现整支队伍排成一条直线或叠在一个点上。

## 阶段 4：交战编排与职业行为

依赖：阶段 3。

工作内容：

- [x] 骑士接敌时围绕敌方前线申请近战交战槽位，每个槽位同时只允许一个攻击者占用。
- [x] 游侠保持 5.5 至 7 个世界单位的作战距离，法师保持 4.5 至 6.2 的作战距离。
- [x] 远程小队受到近战压迫时向本方后方重新找位，不穿过敌方前线。
- [x] 指定集火优先级高于自动目标，目标不可达或死亡后才重新选择。
- [x] 小队存活人数降到初始人数的 30% 时进入撤退，撤退单位不再攻击，沿可通行路线返回本方营地。
- [x] 胜负以可战斗单位为准。一个阵营只剩撤退或死亡单位时判负。
- [x] 调整伤害、射程和移动速度，让默认 AI 对战落在 60 至 100 秒。

测试：

- [x] 两名攻击者不能占用同一个近战槽位。
- [x] 游侠和法师会在职业射程内停止，不主动进入近战距离。
- [x] 撤退单位不会重新获得攻击目标，并能抵达本方营地。
- [x] 目标死亡、路线阻断和小队减员时不会出现 NaN、悬空命令或无限追逐。

验收：

- 接战十秒后仍能分辨前线、远程后排和预备队。
- 败方撤退能在画面中形成明确方向，不会原地消失。

## 阶段 5：补齐攻击、受击、死亡和声音

依赖：阶段 1、阶段 4。

工作内容：

- [x] 箭矢使用可复用对象池，按模拟中的投射物位置飞行。
- [x] 法术命中显示范围预警、爆炸和短暂地面残留，范围与模拟伤害半径一致。
- [x] 近战增加挥砍轨迹、命中火花和小幅受击后仰。
- [x] 单位受击时短暂闪白，低生命时只显示简洁血条，未受伤单位不显示血条。
- [x] 死亡后播放一次死亡动作，尸体保留 6 秒再淡出，不立即从场景移除。
- [x] 小队撤退时切换跑步动作，并显示持续时间不超过两秒的撤退标记。
- [x] 加入本地 Fal 生成音效：箭矢、刀剑、法术、死亡、撤退号角和环境风声。资源与来源说明存入 `public/audio`。
- [x] 加入用户提供的战斗背景、胜利和失败音乐；按当前场景随机播放，所有音乐与音效统一降低 50%，默认关闭并由顶栏总开关控制。
- [x] 同类音效使用对象池和并发上限，避免 40 个单位同时触发造成爆音。
- [x] 大范围法术和小队崩溃触发轻微镜头震动，普通攻击不震动。

测试：

- [x] 每条远程攻击事件只生成一个投射物表现。
- [x] 对象池归还后可以复用，不保留上一次的位置、颜色或目标。
- [x] 死亡和撤退事件不会重复播放声音。

验收：

- 关闭 HUD 后，仅看战场也能判断谁在攻击、谁被命中、哪支队伍正在撤退。
- 连续混战一分钟没有持续增长的场景对象或音频节点。

## 阶段 6：调整 RTS HUD 和操作反馈

依赖：阶段 3、阶段 4。

工作内容：

- [ ] 缩小左右侧栏占用，把主要信息改为覆盖在战场边缘的轻量 HUD。
- [ ] 增加小地图，显示地形轮廓、双方小队、镜头范围和正在撤退的队伍。
- [ ] 选择信息以小队为主，显示兵种、人数、平均生命、当前命令和状态。
- [ ] 命令区提供攻击移动、停止和坚守按钮，并标明快捷键。
- [ ] 地面命令显示阵型落点和朝向，敌军命令显示明确的集火标记。
- [ ] 隐藏状态的胜利面板继续保留 DOM 节点身份，同时保持不可聚焦和不可点击。

验收：

- 战场主体在 1280×720 下至少保留 75% 的可视宽度。
- 真实鼠标按下期间发生周期更新，不会替换或移动正在交互的按钮、画布覆盖层和输入节点。
- 不看说明文字也能完成选兵、移动、攻击移动、集火、停止和坚守。

## 阶段 7：80 单位性能治理

依赖：阶段 5、阶段 6。

工作内容：

- [ ] 为远程后撤、自动追敌和受阻交战槽位建立持久导航意图；只在目标、槽位或阻挡版本变化时重算 A*，其余更新消费缓存路径。
- [ ] 每个模拟 tick 建立一次确定性的阵营空间索引，复用自动目标与近战压力邻域查询，消除逐单位全军排序。
- [ ] GLTF、纹理、几何体和基础材质按资源 URL 共享，不为每个单位重复加载。
- [ ] 特效、投射物、伤害数字和临时标记全部使用对象池。
- [ ] 只有近景角色投射实时阴影，远景角色使用简化阴影或关闭阴影。
- [ ] 健康条、名字和选择圈只在需要时创建或显示。
- [ ] 地形、树木、岩石和重复道具继续使用实例化渲染。
- [ ] 在 `?benchmark=80` 下采集 10 秒数据。如果中位帧时间高于 18.2ms，启用烘焙动画纹理的实例化角色；达到预算则保留共享资源的骨骼动画方案。
- [ ] 生产构建拆分 Three.js 场景代码和界面代码，首次加载页面只显示必要的加载状态。

验收：

- 当前开发机器 1280×720、40v40、持续混战时，中位帧率不低于 55 FPS，1% low 不低于 40 FPS。
- 连续运行五分钟后，活动投射物、临时特效和音频节点数量能回落到空闲基线。
- 生产构建不再出现单个超大入口包警告。

## 阶段 8：整体验收

依赖：前面所有阶段。

自动检查：

- [ ] `pnpm lint`
- [ ] `pnpm typecheck`
- [ ] `pnpm test`
- [ ] `pnpm build`
- [ ] 40v40 完整模拟运行 20 局，不出现 NaN、无限战斗或无法结算。
- [ ] 相同初始状态与命令序列运行两次，胜负、死亡顺序和事件序列一致。

浏览器验收：

- [ ] 用真实屏幕坐标框选一支和多支小队。
- [ ] 分别执行移动、攻击移动、指定集火、停止和坚守。
- [ ] 中键旋转、WASD 平移和滚轮缩放不会触发误选或误下令。
- [ ] 观察一场完整战斗，确认前线、远程站位、投射物、范围法术、死亡、撤退和胜负结算都出现。
- [ ] 重新开局后单位、对象池、镜头、HUD 和声音恢复初始状态。
- [ ] 浏览器控制台没有 warning 或 error。
- [ ] 保存与阶段 0 同机位的三组最终截图，写入 `docs/comparison.md`。

## 实施顺序和质量规则

按阶段 0 到阶段 8 顺序合并。阶段 1 和阶段 2 可以并行开发，其余阶段必须等待依赖项验收完成。

每个行为都走 RED、GREEN、REFACTOR：先写一个会因为缺少该行为而失败的测试，再写最小实现让它通过，最后整理结构并重跑相关测试。每个阶段结束运行一次 `pnpm check`，阶段 8 再做完整浏览器验收。

任何新增的周期更新控件都必须维持 DOM 节点身份。更新期间只原地改变内容和状态，不能用清空容器、重新创建节点或无条件移动节点的方式刷新界面。

## 已知风险

### 80 个骨骼动画角色可能超出帧率预算

建议：阶段 0 先建立数据基线，阶段 7 使用明确的 18.2ms 门槛决定是否切换到烘焙动画实例化。
修复规模：大。
忽略风险：高，特效和声音完成后再处理会造成返工。

### 阵型、避让和 A* 互相争夺单位位置

建议：A* 只给小队生成宏观路线，阵型槽位决定期望位置，局部避让只修正当前速度，不允许三个系统同时直接写最终坐标。
修复规模：大。
忽略风险：高，单位会抖动、堵塞或穿过队友。

### 画面表现可能再次复制战斗规则

建议：所有命中、死亡和撤退表现只消费 `src/game/events.ts` 的事件，不在 Three.js 组件里重新寻找目标或计算伤害。
修复规模：中。
忽略风险：高，弹道、动画和实际扣血会再次不一致。

### 大量内容同时加入会让战斗失去可读性

建议：每个阶段都用固定机位截图验收。先确认阵型和前线清楚，再增加粒子、血条和 HUD。
修复规模：小。
忽略风险：中，画面会很热闹，但玩家无法判断局势。

## 开工条件

- 问题理解：清楚。目标是先做一场接近参考站观感、同时保留玩家控制的 40v40 战斗。
- 技术路线：清楚。沿用 React、TypeScript、Three.js 和 React Three Fiber，不增加后端与新语言。
- 外部依赖：清楚。视觉资源继续复用 KayKit；声音由 Fal `sonilo/v1.1/text-to-sound-effects` 生成并保存为本地 WAV，游戏运行时不需要账号、API key 或在线服务。
- 未知项：已转成阶段 0 的可测数据，主要是 80 个骨骼动画角色的性能上限，不阻塞地图、事件和阵型工作。
