# Battlefield RTS 沙盒模式执行计划

> 状态：大地图规则拓扑已锁定，精细地块与八个完整矿区已接入；人口与经济基线待试玩确认
>
> 工作分支：codex/sandbox-mode
>
> 地图版本：Sandbox Large Battlefield v2
>
> 文档日期：2026-08-20
>
> 当前进度：仅完成设计与计划，尚未修改游戏代码

本文件替代此前的“自动部署玩法开发计划”。旧计划仍保留在 Git 历史中，不在当前文档里同时维护两套互相冲突的规则。

---

## 1. 最终目标

沙盒模式不是“去掉倒计时的现有模式”，而是一套独立的 RTS 遭遇战：

- 开局 1000 金币，没有被动收入。
- 玩家必须占领有限矿坑并建造金矿，才能持续获得金币。
- 建筑不会随时间自然掉血，但仍会受到战斗伤害并被摧毁。
- 玩家先建造兵营，再用相应高级建筑解锁并训练高级兵种。
- 单位只能逐个从对应生产建筑的出口生成，不能从部署栏直接投放。
- 所有玩家单位由玩家手动选择目标点，不自动冲向敌方城堡。
- 敌方 AI 使用与玩家相同的金币、矿坑、科技、队列、人口和命令规则。
- 每方拥有 100 人口硬上限；超过 50 和 80 人口后，矿产净收入分别降为 80% 和 60%。
- 无比赛时间上限；摧毁敌方城堡获胜，资源完全枯竭且双方都无法再形成进攻能力时判定僵局。
- 沙盒使用一张 871 格的大地图；三条主路线、八个矿坑和双方建造区都由显式地图数据定义。
- campaign、normal、arena 及内部 infinite 基准模式的现有行为保持不变。

### 1.1 MVP 完成闭环

首个可玩版本必须能完整完成以下流程：

1. 在模式选择器进入“沙盒模式”。
2. 在玩家安全矿坑建造金矿。
3. 建造兵营并训练基础单位。
4. 建造至少一种高级生产建筑并训练对应单位。
5. 手动选择、编队、换线、占领中立矿坑。
6. 通过有限矿产继续扩军。
7. 由玩家下令摧毁敌方城堡，或触发严格的资源僵局判定。

只完成“大地图展示”或“去掉计时器”不算完成沙盒模式。

---

## 2. 当前玩法规则基线

### 2.1 模式与经济

| 项目 | Sandbox v1 规则 |
|---|---|
| 初始金币 | 1000 |
| 金币上限 | 5000 |
| 被动金币 | 无 |
| 比赛倒计时 | 无 |
| 建筑自然掉血 | 无 |
| 建筑战斗伤害 | 正常生效 |
| 金矿成本 | 400 |
| 金矿建造时间 | 6 秒 |
| 金矿开采量 | 每 4 秒最多开采 100 原矿 |
| 单坑最大储量 | 3000 原矿，标准情况下 30 次完整开采 |
| 满仓行为 | 暂停产出，不消耗矿坑储量 |
| 矿坑记账 | 储量属于矿坑，不属于金矿建筑 |
| 矿坑重建/易手 | 剩余储量不重置 |
| 枯竭 | 永久枯竭；枯竭矿可拆除 |
| 每方人口硬上限 | 100 人口点 |

经济更新必须使用确定性模拟时钟，不以 React 渲染帧或动画回调作为结算依据。

### 2.2 人口上限与军备维护

人口按阵营分别计算。100 是固定硬上限，Sandbox v1 不使用住房或扩容建筑。

| 现役人口 | 矿产收入倍率 | 每矿每 4 秒 | 每矿每分钟 | 单坑全程理论净收入 |
|---|---:|---:|---:|---:|
| 0–50 | 100% | 100 | 1500 | 3000 |
| 51–80 | 80% | 80 | 1200 | 2400 |
| 81–100 | 60% | 60 | 900 | 1800 |

边界固定为“超过 50”和“超过 80”：50 人仍为 100%，51 人进入 80%；80 人仍为 80%，81 人进入 60%。

人口状态分为：

- usedPopulation：已经训练完成的单位人口，包括已在场单位和因出口阻塞而处于 ready-blocked 的成品。
- reservedPopulation：正在排队或训练的单位人口。
- committedPopulation = usedPopulation + reservedPopulation。
- 入队条件：committedPopulation + 新订单人口 ≤ 100；每个订单严格对应一个单位。
- 采矿倍率只读取 usedPopulation；未完成的队列不提前收维护费。
- 训练完成时，人口从 reserved 转入 used；此时即使出口被堵，也会影响下一次矿产结算。
- 单位在权威死亡结算时立即释放人口，不等待死亡动画结束。
- 建筑、城堡、防御塔、弹道物和纯视觉召唤物不占人口。

沙盒不再存在兵团编制；每个订单只生成一个可独立选择和下令的单位。人口值为：

| 单位 | 每单生成 | 人口占用 |
|---|---:|---:|
| 长枪兵 | 1 | 1 |
| 剑士 | 1 | 1 |
| 弓箭手 | 1 | 1 |
| 法师 | 1 | 2 |
| 投石车/对应重型单位 | 1 | 4 |

维护费是从新开采的原矿中截留，不从已有钱包周期扣款：

    grossExtracted = 本次从矿坑开采的原矿
    upkeepWithheld = grossExtracted × (1 - incomeRate)
    netCredited = grossExtracted - upkeepWithheld
    pit.remainingOre -= grossExtracted
    wallet.gold += netCredited

完整开采 100 原矿时：

- 0–50 人口：矿坑 -100，钱包 +100，维护 0。
- 51–80 人口：矿坑 -100，钱包 +80，维护 20。
- 81–100 人口：矿坑 -100，钱包 +60，维护 40。

因此维护会消耗有限矿藏的总价值，而不只是延长采矿时间。单坑无中断开采时间仍为 120 秒，但全程处于三个档位时，最终净收入分别为 3000、2400 和 1800。

结算细节：

- 每次矿产结算读取该时刻的 usedPopulation，不使用队列预留人口。
- 同一模拟时间戳固定按“伤亡与生产完成 → 重算人口 → 矿产结算”的顺序处理。
- 多座矿同一时刻结算时按稳定矿坑 ID 排序，保证重放一致。
- 钱包已满时暂停，gross、maintenance 和 net 均为 0。
- 钱包空间不足当前完整净收入时，按可容纳净收入同比例减少 gross；不得吞掉原矿。
- 内部使用确定性定点数或余数累计，UI 显示整数金币，禁止浮点漂移。
- 减员跨回较低档位后，下一次矿产结算立即恢复更高收入率。
- MVP 不提供解散单位或手动暂停金矿，避免围绕结算时点反复切换人口档位。

账本必须满足：

    reserveConsumed = grossExtracted
    grossExtracted = upkeepWithheld + netCredited
    walletIncrease = netCredited

这套设计使 0–50 成为高效经济线、51–80 成为扩张军力线、81–100 成为需要广泛矿区控制的优势军队线；100 是极限上限，不要求每局都自然打满。

100/50/80 与 100%/80%/60% 是本轮首版执行基线。阶段 9 如果试玩证明 51 或 81 的收入断崖过强，只能通过一次显式平衡决策调整，并同步更新 UI、AI、经济模拟和边界测试，不能在实现中暗改。

### 2.3 矿坑控制与建矿

- 一个矿坑同时最多承载一座金矿。
- 金矿只能放在矿坑中心锚点；普通格不能建金矿。
- 矿坑中心不能建其他建筑。
- 玩家区两座安全矿开局属于玩家，敌方两座安全矿开局属于敌方。
- 四座中立矿开局无归属。
- 占领条件：至少一名存活士兵进入矿坑中心或周边 1 格，立即取得该矿坑控制权。
- 双方士兵同时位于占领范围时，先到的一方（即当前控制方）继续控制；只有该方范围内士兵全部阵亡或离开后，仍在范围内的另一方才能接管。同一模拟步首次同时进入时按稳定单位顺序裁决，保证重放确定性。
- 矿坑控制权只决定哪一方可以新建金矿，不决定现有金矿的阵营与采集归属，也不会自动停止仍存活金矿的采集。
- 已有敌方金矿会阻止换方占领；进攻方必须先摧毁原金矿，摧毁结算后才能接管，建筑占位清理后才能新建金矿。
- 矿坑的一格保护圈保持可行走，但禁止普通建筑，防止用建筑封死入口。
- 每个矿坑保留两个独立道路入口。

### 2.4 防软锁安全阀

Sandbox v1 锁定使用一次性“应急矿许可证”，并对玩家与 AI 对称开放。只有同时满足以下条件时可用：

- 当前金币少于 400。
- 没有存活或在建的己方金矿。
- 至少控制一个尚未枯竭且未被占用的矿坑。
- 本局尚未使用过应急许可证。

许可证只免除一次金矿的 400 金币建造费，不增加矿坑储量，也不绕过建造时间、控制权或占用检查。阶段 4 的确定性经济与软锁场景验证确认保留许可证方案：它只在“低于 400、无存活或在建金矿、仍控制可采空坑、尚未使用”四项同时成立时触发，不需要把“最后 400 金币不得消费”的隐式限制扩散到阶段 5 的所有消费事务。实际 UI 文案与可发现性继续在阶段 5/8 联调，但规则不再二选一。

### 2.5 建筑与科技树

基础科技关系锁定为：

    城堡
    ├─ 金矿
    └─ 兵营
       ├─ 训练：长枪兵、剑士
       ├─ 解锁：靶场 → 训练弓箭手
       ├─ 解锁：法师塔 → 训练法师
       └─ 解锁：攻城工坊 → 训练投石车

规则：

- 高级生产建筑开工时必须存在至少一座已建成且存活的兵营；在建兵营不算解锁。
- 兵营被摧毁后，已经建成或已经合法开工的高级生产建筑仍可继续完成和训练。
- 没有已建成且存活的兵营时，不能新开工高级生产建筑。
- 人类与亡灵共用相同的生产槽位、队列和科技规则；名字、模型、动画和兵种映射由种族目录决定。
- 守卫塔作为防御建筑，Sandbox v1 每方最多 2 座，只能建在核心建造区；默认由兵营解锁。
- 所有普通建筑在规则层使用一格锚点。视觉模型可以越出该格，但不得偷偷扩大碰撞或封路。
- 建造中的工地可被攻击；完成后不会自然衰减。
- MVP 不提供自动维修、工人、自由城墙或建筑升级。

阶段 5 已将首版建筑数值锁入集中沙盒目录：

| 建筑槽位 | 费用 | 建造时间 | 生命值 | 数量限制 |
|---|---:|---:|---:|---:|
| 金矿 | 400 | 6 秒 | 750 | 每坑 1 座 |
| 兵营 | 400 | 8 秒 | 1200 | 无额外上限 |
| 靶场 | 500 | 10 秒 | 1000 | 无额外上限 |
| 法师塔 | 700 | 12 秒 | 900 | 无额外上限 |
| 攻城工坊 | 800 | 16 秒 | 1400 | 无额外上限 |
| 守卫塔 | 500 | 8 秒 | 450 | 每方 2 座 |

1000 初始金币可以完成“金矿 400 + 兵营 400 + 两名长枪兵 200”的第一条经济与生产闭环。这些数值不得散落到 UI 或场景组件中。

### 2.6 单位训练

“训练一次”严格生成一个独立单位：

| 生产建筑 | 单位 | 费用 | 训练时间 | 人口占用 |
|---|---|---:|---:|---:|
| 兵营 | 长枪兵 | 100 | 3 秒 | 1 |
| 兵营 | 剑士 | 140 | 4 秒 | 1 |
| 靶场 | 弓箭手 | 160 | 5 秒 | 1 |
| 法师塔 | 法师 | 300 | 7 秒 | 2 |
| 攻城工坊 | 投石车/对应重型单位 | 700 | 16 秒 | 4 |

队列规则：

- 每座生产建筑使用 FIFO 队列，最多 5 项，正在训练和等待出口的项目都计入上限。
- 入队成功时立即扣款且只扣一次。
- 人口在入队时预留；used、reserved 与新单位人口之和超过 100 时拒绝入队。
- MVP 不允许取消或调整队列，避免在首版引入退款和优先级歧义。
- 生产建筑被摧毁时，所有未生成队列清空：排队/训练项释放 reserved，ready-blocked 成品释放 used。Sandbox v1 已锁定不退款，避免摧毁/重建产生经济套利。
- 训练完成但出口被堵时，项目进入 ready-blocked 状态；不丢失、不跳队、不重复扣款。
- 出口恢复后按原顺序生成。
- 每个生产建筑定义“相邻门口 1 格 + 第二环 3 格”的四格出口扇形，并且至少有一个出口格能连接 RoadReserve。
- 新单位默认 idle；若建筑设置了集结点，则先移动到集结点，抵达后回到 idle。
- 每方人口上限为 100 人口点，不在 MVP 中加入住房系统。

### 2.7 玩家手动命令

单个单位是最小选择和命令单位，不存在共享选择或共享生命的兵团。

桌面端首版命令：

| 输入 | 结果 |
|---|---|
| 左键单击单位 | 选择一个己方单位 |
| 左键拖框 | 框选多个己方单位 |
| Shift + 选择 | 增加或移除单个单位 |
| 选中后点击地面 | 向目标点索敌前进 |
| Esc | 退出建造预览或清空待定输入 |

命令语义：

- 任何单位都会自动攻击自身攻击范围内的敌人。
- 点击地面后，单位保存目标点并一边索敌一边前进。
- 沿路遇敌时先暂停行军并消灭敌人，交战结束后恢复原目标点。
- 多选下令时，每个单位获得独立、确定性的编队落点，不能共享控制 ID。

玩家目标点与局部战斗目标必须分离。局部目标死亡后，单位要恢复原目标点，而不是进入旧的自动冲城行为。

移动端同样采用“选择单位 → 点击目标点”；单指拖动仍用于相机。首版不要求在小地图上下达单位命令。

### 2.8 AI 与结算

- AI 是“使用同一公开事务接口的自动玩家”，不得调用免费部署或直接写状态。
- AI 的金币必须来自初始资金或矿坑。
- AI 必须遵守建造区、前置科技、训练时间、人口、出口阻塞和有限储量。
- AI 承受完全相同的 100 人口上限和 100%/80%/60% 矿产收入档位，难度不得修改倍率。
- 首版使用确定性阶段机和固定排序，不依赖隐藏随机优势。
- 推荐开局顺序：安全矿 → 兵营 → 基础单位 → 高级生产建筑 → 第二矿 → 集结进攻。
- 矿被毁或枯竭后，AI 转入 Recovery/Rebuild，并寻找最近的合法矿坑。
- 无时间限制。
- 单方城堡被摧毁时，对方立即获胜。
- 同一模拟步双方城堡同时被摧毁时判平局。
- 首版不提供“胜利后继续”；结算后冻结经济、建造、训练、命令和 AI，只保留短暂表现动画。
- 结算结果增加 resolutionReason：castle-destroyed、simultaneous-destruction 或 resource-stalemate。

资源僵局必须保守判定：只有八座矿坑总储量全部耗尽，并且双方均没有存活的可进攻单位、有效弹道、训练中或排队单位、可完成的在建生产建筑，也没有足够金币通过现有或可建科技链重新获得进攻能力时，才判 resource-stalemate。只要任一方仍有破城或恢复生产的可能，就不能提前结算。

### 2.9 实施前仍需锁定的平衡项

地图拓扑和核心系统规则已经锁定。剩余三类配置不阻塞架构与灰盒工作，但必须在对应阶段开工前定稿：

| 决策 | 当前计划默认值 | 最迟锁定阶段 |
|---|---|---|
| 防软锁方式 | 双方各一次应急矿许可证 | 阶段 4 |
| 兵营/高级建筑/守卫塔的费用、工期、生命值 | 已锁入 2.5 首版表和集中目录 | 阶段 5（已锁定） |
| 生产建筑被毁后的队列退款 | 清空队列、不退款、释放人口预留 | 阶段 5 |

这些值必须进入集中规则表、验证器和测试，不得以组件默认参数留到发布时再决定。

---

## 3. 大地图灰盒规格

### 3.1 坐标、边界与尺寸

沿用项目当前轴向六边格坐标。定义：

    u = 2q + r
    D = max(|q|, |r|, |q+r|)

地图单元满足：

    D ≤ 18
    |u| ≤ 24

锁定结果：

- 恰好 871 个唯一格子。
- 当前地图为 213 格；新地图面积为其 4.09 倍。
- 相邻六边格中心距离仍为 2 个世界单位。
- 世界中心范围：x = [-24, 24]。
- 世界中心范围：z = [-31.177, 31.177]。
- 世界跨度约为 48 × 62.354。
- 不通过改变六边格尺寸来调节行军时间。

### 3.2 基地、城门与集结点

| 阵营 | 城堡 | 双格城门 | 基地集结点 |
|---|---|---|---|
| 玩家/verdant | (-8, 16) | (-8, 15)、(-7, 15) | (-7, 14) |
| 敌方/crimson | (8, -16) | (8, -15)、(7, -15) | (7, -14) |

敌我几何必须精确中心镜像。城堡、城门、集结点、生产出口和相机重置点都来自地图定义，不允许场景层重新推导。

### 3.3 三条主路线

三路定位：

- 中央主路：最短、最开阔、没有矿坑，是正面交战线。
- 西侧矿山路：连接矿坑群和关口，是资源争夺线。
- 东侧林地/河岸路：最长侧翼，提供绕行和换线空间。

路线在中段以 u = -12、0、+12 为中心，在 |r| = 10…14 内向双方基地扇形汇聚。

定义外侧路线中心偏移：

    A(r) = 12                  当 |r| ≤ 10
    A(r) = 42 - 3|r|          当 10 < |r| ≤ 14

主路格满足：

    |r| ≤ 14
    min(|u|, |u-A(r)|, |u+A(r)|) ≤ 3

基地门前路：

    |r| = 15 且 |u| ≤ 4

两条横向联络带：

    (|r-5| ≤ 1 或 |r+5| ≤ 1) 且 |u| ≤ 17

门前 apron 额外保留：

    |r| ∈ {14, 15} 且 |u| ≤ 4

其中 r = ±14 额外增加四个保留格：

- 玩家侧：(-9, 14)、(-5, 14)。
- 敌方镜像：(9, -14)、(5, -14)。

验收口径：

- 路线拓扑本体为 327 格。
- 加上四个门前 apron 格后，RoadAndExitReserve 共 331 格。
- 全部保留格可行走、不可建造、不可放固定障碍或装饰碰撞。
- 每条主路任一横截面至少 3 格宽。
- 南北路网最小顶点割为 3；移除任意 1 或 2 个非端点道路格都不能完全断开战线。
- 单一路口、单格桥或装饰模型不得成为唯一通道。

锁定的道路距离：

| 路线 | 六边格步数 | 世界距离 |
|---|---:|---:|
| 城门到城门，中路 | 30 | 60 |
| 城门到城门，西路 | 38 | 76 |
| 城门到城门，东路 | 38 | 76 |
| 相邻路线换线 | 6 | 12 |
| 西侧到东侧换线 | 12 | 24 |

用于路径属性测试的参考点：

- 中路：玩家门 → (-7,14) → (-5,10) → (5,-10) → (7,-14) → 敌方门，共 30 步。
- 西路：(-8,15) → (-7,14) → (-11,10) → (-1,-10) → (7,-14) → (7,-15)，共 38 步。
- 东路：(-7,15) → (-7,14) → (1,10) → (11,-10) → (7,-14) → (8,-15)，共 38 步。

### 3.4 八个矿坑

每坑原矿容量均为 3000，且都有两个与矿坑相邻、属于道路并可达的入口：

| ID | 区域 | 矿坑中心 | 两个入口 | 玩家城门最短距离 |
|---|---|---|---|---:|
| P-W | 玩家安全矿 | (-13, 10) | (-12,10)、(-12,9) | 9 |
| P-E | 玩家安全矿 | (3, 10) | (2,10)、(3,9) | 9 |
| N-NW | 近端中立矿 | (-11, 4) | (-10,4)、(-11,5) | 13 |
| N-NE | 近端中立矿 | (7, 4) | (6,4)、(6,5) | 13 |
| N-SW | 远端中立矿 | (-7, -4) | (-6,-4)、(-6,-5) | 20 |
| N-SE | 远端中立矿 | (11, -4) | (10,-4)、(11,-5) | 20 |
| E-W | 敌方安全矿 | (-3, -10) | (-2,-10)、(-3,-9) | 25 |
| E-E | 敌方安全矿 | (13, -10) | (12,-10)、(12,-9) | 25 |

这等于 6 个非敌方矿坑和 2 个敌方矿坑，满足“大地图除敌方地盘外有 4–6 处矿坑”的要求。

### 3.5 基地建造区

玩家普通建造 envelope：

    13 ≤ r ≤ 17
    3 ≤ hexDistance(cell, verdantCastle) ≤ 6

敌方普通建造 envelope：

    -17 ≤ r ≤ -13
    3 ≤ hexDistance(cell, crimsonCastle) ≤ 6

从 envelope 中排除：

- RoadAndExitReserve。
- 与城堡六边格距离不超过 2 的保护区。
- 与任一矿坑六边格距离不超过 1 的保护区。
- 双城门及门前 apron。
- 八个矿坑外侧的显式矿山岩脊阻挡格。
- 固定结构和已经占用的建筑锚点。

锁定结果：

- 每方恰好 34 个普通建筑锚点，全部聚集在城堡 3–6 格范围内。
- 玩家 r = 13…17 每行分别为 4、6、8、8、8 个。
- 左翼 17 个，右翼 17 个。
- 敌方为精确镜像。
- 中立区禁止普通建筑；只有矿坑中心可建金矿。
- MVP 不允许自由城墙，不允许用建筑封住道路、关口或生产出口。

### 3.6 相机与小地图

- 初始相机目标：玩家集结点 (-7,14)。
- 初始 zoom：32。
- 最大 zoom：56。
- overview 最小 zoom 根据地图世界边界、视口比例和一格边距动态计算，不再与初始 zoom 绑定。
- Reset Camera 返回玩家基地，而不是地图中心。
- 小地图固定“玩家在下、敌方在上”的朝向。
- 小地图必须显示：地图边界、三条道路、建造区、八坑状态、城堡、建筑、己方/敌方单位和当前视口框。
- Sandbox v1 无战争迷雾，小地图显示全部状态。
- 小地图首版支持点击跳转相机，不支持直接下达部队命令。
- 桌面端建议尺寸约 180 × 234；移动端必须在 390 × 844 基准视口内不遮住主要命令。

### 3.7 行军时间预算

测试条件：固定 1/60 秒模拟步长、无战斗、无动态阻挡，从命令被接受计时到抵达目标。

| 路程 | 长枪兵 | 剑士 | 弓箭手 | 法师 | 投石车 |
|---|---:|---:|---:|---:|---:|
| 自家矿，9 步 | 6.5s | 6.9s | 6.3s | 7.3s | 13.3s |
| 近中立矿，13 步 | 9.5s | 10.0s | 9.1s | 10.6s | 19.3s |
| 远中立矿，20 步 | 14.5s | 15.4s | 14.0s | 16.3s | 29.6s |
| 敌方矿，25 步 | 18.2s | 19.2s | 17.5s | 20.4s | 37.0s |
| 中路，30 步 | 21.8s | 23.1s | 21.1s | 24.5s | 44.4s |
| 侧路，38 步 | 27.6s | 29.2s | 26.7s | 31.0s | 56.3s |
| 相邻换线，6 步 | 4.4s | 4.6s | 4.2s | 4.9s | 8.9s |

验收：

- 上述 35 个组合的实际时间误差均不超过 ±8%。
- 最快亡灵长枪兵的半程中路反应时间约 9.5 秒，同样执行 ±8% 验收。
- 当前 0.55 的 waypoint 到达阈值可能令实际移动提前约 15%–22%；应修正移动积分、路径采样或抵达判定。
- 不允许通过扩大地图、修改格距或整体降低兵种速度掩盖到达阈值问题。
- 第一版不改现有兵种移速。

---

## 4. 架构与数据边界

### 4.1 模式定义，而不是布尔分支

现有 MATCH_POLICIES.infinite 只负责“无计时”，不能作为沙盒别名。新增 BattleModeDefinition，并至少组合以下策略：

- mapId：选择地图定义。
- clockPolicy：计时与超时结算。
- economyPolicy：初始金币、上限、被动收入、原矿开采、人口收入档位和维护截留。
- buildingLifecyclePolicy：自然衰减、建造过程、维修。
- acquisitionPolicy：直接部署或建筑生产。
- controlAuthority：自动战斗或玩家/AI 命令。
- productionPolicy：科技、队列、100 人口上限、人口预留和出口。
- opponentPolicy：现有部署 AI 或沙盒 RTS AI。
- victoryPolicy：超时、城堡摧毁、僵局。
- uiLayout：部署栏或沙盒建造/生产/命令界面。

旧模式继续引用原值。沙盒的任一策略都不得通过修改全局 GAME_RULES 默认值来实现。

BattleState 只保存可序列化的 modeId 和 mapId，不保存 Map、WeakMap 或编译索引。纯模拟函数通过 BattleRuntimeContext 获得已解析的 mode、map 和 navigation。现有规则校验器中“被动恢复必须为正数”“建筑 lifetime 必须为正数”等假设，要改为显式支持 disabled/null，不能用 0 冒充关闭。

### 4.2 静态地图定义

引入不可变的 BattlefieldDefinition，建议包含：

- id、version、displayName。
- cells 或确定性的 cell generator。
- worldBounds、center、castles、gates、rallyPoints、spawns。
- zones：核心区、中立区、敌区及显式 buildPolicy。
- routes：RoadReserve、联络带、生产出口连接点。
- minePits：中心、保护圈、入口、初始归属、容量。
- fixedStructures、visualModules、cameraPreset、minimapPreset。

地图逻辑不再根据 r ≥ 2 或 r ≤ -2 推导阵营领土；每格的 zoneId、buildPolicy、routeTags 和 blocker 类型都来自地图定义。

### 4.3 运行时索引

从定义构建 BattlefieldRuntimeIndex：

- cellByKey：坐标到格子的 O(1) 查询。
- neighborKeys：预计算邻接。
- walkableComponents：静态连通分量。
- routeCells、buildAnchorSets、pitByKey、structureFootprints。
- world/axial/minimap 坐标转换所需边界。
- dynamicBlockers 与单调递增的 navigationRevision。

getMapCell 的数组 find 不得继续出现在寻路、放置预览或每帧战斗热路径中。

寻路缓存键至少包含 mapId、navigationRevision、起点、终点和 movementMode。建筑开工、落成、摧毁或拆除改变动态阻挡时必须推进 revision，使旧路径自动失效。nearestWalkable 使用邻域扩张，不能每次对全地图排序；每个沙盒单位保留独立目标和 formation slot。

### 4.4 规则状态

建议增加以下显式状态，而不是从场景对象反推：

- MinePitState：remainingOre、controller、captureProgress、occupyingMineId、depleted。
- PopulationState：usedPopulation、reservedPopulation、cap、incomeRate。
- BuildingState：phase、constructionRemaining、health、anchor、exitCells、rallyPoint。
- ProductionQueueState：entries、activeProgress、readyBlocked。
- SquadState：memberIds、owner、order、localCombatTarget。
- SquadOrder：idle、move、attack、attackMove、hold。
- SandboxAiState：phase、currentGoal、retryAt、deterministicSequence。
- BattleResolution：winner、resolutionReason、resolvedAt。

静态 MinePitDefinition 与运行时 MinePitState 必须分开，避免摧毁金矿时意外恢复储量。

selectedUnitIds（当前底层以一对一兼容控制 ID 保存）、框选矩形、当前输入工具和指针状态只属于 UI，不进入权威 BattleState；已接受的单位订单、建造与生产队列才进入模拟状态。这样既能确定性测试，也不会把临时鼠标状态写进回放所需数据。

### 4.5 统一事务接口

玩家 UI 与敌方 AI 必须调用相同的纯规则入口：

- canStartConstruction / startConstruction。
- canClaimPit / updatePitCapture。
- enqueueProduction。
- resolveMineProduction。
- setRallyPoint。
- issueSquadOrder。
- advanceBattle。

每个入口返回明确的成功结果或原因码，例如 insufficient-gold、missing-prerequisite、invalid-zone、pit-not-controlled、population-cap、queue-full、exit-blocked、no-path。UI 只翻译原因，不复制规则。resolveMineProduction 同时返回 populationSnapshot、incomeRate、grossExtracted、upkeepWithheld 和 netCredited，供账本、AI 和 UI 共用。

### 4.6 表现层隔离

- React Three Fiber 场景只渲染 BattleState 和 BattlefieldDefinition。
- 视觉 props、森林、河流、矿车和建筑模型不能决定碰撞、资源或建造合法性。
- 逻辑地形、道路、触发区、建筑锚点、资源实体和视觉装饰分层管理。
- 大地图地块优先实例化或批量渲染；不能为 871 格创建 871 套独立高成本更新逻辑。
- 美术阶段只能填充已经锁定的地图槽位；任何道路、矿坑入口、地形阻挡或建造锚点调整都必须作为显式设计变更并重跑几何测试。

### 4.7 迁移兼容

- 当前 BATTLEFIELD_MAP 在迁移期保留为 legacy 默认适配器。
- BattleSession 创建时必须显式获得 modeDefinition 和 mapDefinition，并存入会话上下文。
- 战斗、部署、建造、导航、相机、雾效、灯光、阴影、水面、场景和 UI 逐一改为读取会话地图。
- 清理 autoCombat 中未包含 mapId/navigationRevision 的城堡路线缓存。
- 非规则六边形边界的外围水裙必须从真实边界邻格生成，不能继续假设 radius + 1 圆环。
- 同一进程连续创建旧地图战局和沙盒战局时，任何 WeakMap 缓存、单例 store 或场景桥接状态都不能串局。
- 历史提交 d5a0754 中的 selection、battlePlans、命令标记和相机桥接可作为参考移植，但不可直接回滚；旧实现按单位 ID 工作，也带有已经废弃的战前军令假设。

---

## 5. 实施顺序

状态标记：

- [ ] 未开始
- [~] 进行中
- [x] 完成并通过阶段门

每个阶段单独提交。任何阶段在自动测试、手工验收或旧模式回归未通过时，不进入下一阶段。

### 阶段 0：固化基线与回归护栏

状态：[ ]

目标：先证明现有分支是可复现的绿色基线，并把沙盒将要改变的旧行为锁在旧模式测试里。

工作项：

1. 运行并记录 pnpm check、pnpm arena 和当前 benchmark。
2. 固化旧地图 213 格、原始城堡和部署区坐标。
3. 固化旧模式初始金币、被动恢复、建筑自然衰减、直接部署、自动冲锋和计时结算。
4. 为同进程创建两场 BattleSession 增加隔离测试。
5. 记录桌面和 390 × 844 移动视口的现有关键截图。
6. 建立沙盒测试 builder 和确定性模拟时钟，但不在 UI 暴露沙盒入口。

阶段门：

- pnpm check 全绿。
- pnpm arena 全绿。
- 当前 80 单位 benchmark 数字被记录，后续性能比较有基线。
- 没有产品行为变化。

建议提交：test(sandbox): lock legacy battle baseline

### 阶段 1：ModeDefinition 与 MapDefinition 注入

状态：[~]（实现完成；`pnpm check` 全绿。`pnpm arena` 的 2 项既有数值断言在基线提交 `f6cf06b` 上同样失败，因此阶段门暂不签核。）

依赖：阶段 0。

目标：解除“全局地图 + 单一规则表”耦合，不改变任何现有玩法。

工作项：

1. 为 GameMode 增加独立 sandbox 值；保留 infinite 作为内部无计时策略，二者不等价。
2. 建立 BattleModeDefinition 注册表。
3. 建立 BattlefieldDefinition 注册表和 legacy map adapter。
4. BattleSession/BattleState 创建入口显式注入模式与地图。
5. 逐步消除规则层对 BATTLEFIELD_MAP 的直接导入。
6. 将 economy、population/upkeep、building lifecycle、unit acquisition、control authority、AI 和 victory 读取改为策略。
7. 为场景层提供只读 session context；不得让组件自己按 mode 拼规则。
8. Sandbox 在内部测试可创建，但模式选择器继续隐藏，直到阶段 8。

重点触达：

- src/app/gameMode.ts
- src/game/rules.ts
- src/game/battle.ts
- src/game/battleSession.ts
- src/game/economy.ts
- src/game/buildings.ts
- src/game/deployment.ts
- src/map/battlefield.ts
- src/scene/BattlefieldCanvas.tsx

阶段门：

- campaign、normal、arena、infinite 的序列化状态和关键行为不变。
- Sandbox 定义明确包含 100 人口 cap 与三档 populationIncomeBands，旧模式没有隐式维护费。
- 同一进程先后创建 legacy 与 sandbox 测试会话，不发生地图、经济、相机或 AI 状态污染。
- 禁止以 if (mode === sandbox) 散落到组件作为主要架构。
- pnpm check 与 pnpm arena 全绿。

建议提交：refactor(battle): inject mode and map definitions

### 阶段 2：871 格大地图灰盒与导航

状态：[~]（实现完成；`pnpm check` 全绿，灰盒实画面与阶段二几何、导航、行军时间门均通过。`pnpm arena` 仍是阶段一已确认在基线提交 `f6cf06b` 上存在的 2 项数值断言失败，未新增失败项，因此暂不签核全局阶段门。）

依赖：阶段 1。

目标：先完成纯数据和灰盒可视化，锁定边界、三路、八矿和双方建造区。

工作项：

1. 新建 sandbox-large-v1 地图定义与纯函数生成器。
2. 生成 871 个格子、331 个道路/出口保留格、城堡、双门、集结点、八坑和 34 × 2 城堡邻近建造锚点。
3. 为地图定义生成稳定 fingerprint；任何坐标变化必须显式更新测试。
4. 建立 cellByKey 和邻接索引，移除导航热路径中的线性 cell 查找。
5. 将 A* open set 改为优先队列，并将 nearestWalkable 改为邻域扩张。
6. 路径缓存带 mapId 和 navigationRevision；建筑变化后旧缓存必须失效。
7. 每个单位独立寻路；先测量再决定是否加入多单位路径复用或 flow field。
8. 动态建筑只影响运行时阻挡层，不修改静态地图定义。
9. 灰盒阶段先渲染单色地形和路线层；精细化后复用官方 KayKit 六边格，并将 123 格视觉道路中心线与 327 格行军走廊分离。
10. 八个矿坑使用左右与阵营镜像矿山岩脊；64 个岩脊格在逻辑层登记为 terrain blocker，入口和道路保持畅通。

自动属性测试：

- 每格满足 D ≤ 18 和 |u| ≤ 24。
- 坐标唯一且格数恰好 871。
- 世界边界精确符合设计。
- 邻接双向一致；所有可走区属于预期连通分量。
- 城堡、门、集结点、矿坑和入口坐标逐项断言。
- 路线本体 327 格，加入 apron 后保留区 331 格。
- 所有保留格可走、不可建、无固定阻挡。
- 穷举移除任意 1 或 2 个非端点道路格，南北仍连通。
- 三路距离 30/38/38，换线距离 6/12。
- 八坑入口均在界内、相邻、属于道路且可达。
- 玩家至四级矿坑距离为 9/13/20/25，敌方镜像。
- 每方建造锚点 34，行分布 4/6/8/8/8，左右各 17，全部距城堡 3–6 格。

阶段门：

- 所有几何数字由自动化测试保护。
- 在灰盒画面可从基地沿三路到达敌方基地。
- 任一主路不存在一格瓶颈。
- 1000 次预热后的随机可达寻路成功率 100%；参考目标 p95 ≤ 3ms、单次最大值 ≤ 10ms。
- 未改任何单位速度。

建议提交：feat(map): add sandbox large graybox definition

### 阶段 3：大地图相机、小地图与场景边界

状态：[~]（实现完成；`pnpm check` 全绿，相机、小地图、真实边界水裙、390 × 844 布局和六边形无缝咬合均通过。`pnpm arena` 仍仅有阶段一已确认的 2 项基线数值断言失败，未新增失败项，因此暂不签核全局阶段门。）

依赖：阶段 2。

目标：让 871 格地图真正可观察和可操作，而不是只能渲染。

工作项：

1. 相机约束从 BattlefieldDefinition.worldBounds 动态计算。
2. 解耦 startZoom、overviewMinZoom 和 pan progress。
3. 应用初始目标 (-7,14)、zoom 32、max 56。
4. Camera Reset 回到玩家基地；敌方视角测试使用镜像 preset。
5. 固定灯光、阴影、水面底板、雾效和部署遮罩的范围改为地图驱动。
6. 外围水裙从真实地图边界邻格生成，验证裁切地图边缘不露底。
7. 新建小地图坐标投影和反投影。
8. 绘制边界、道路、建造区、八坑、城堡、建筑、单位和视口框。
9. 支持点击小地图移动相机；事件不能穿透为战场命令。
10. 增加桌面与移动布局测试。

阶段门：

- overview 可完整容纳地图及至少一格边距。
- 初始视图聚焦玩家基地和安全矿，不暴露错误的中心开局。
- 小地图世界坐标往返误差小于半格。
- 点击四角、双方城堡和八个矿坑均定位正确。
- 390 × 844 视口中小地图不遮住主要资源与命令入口。

建议提交：feat(scene): add sandbox camera and minimap

### 阶段 4：矿坑、有限经济与永久建筑生命周期

状态：[~]（实现完成；`pnpm check` 全绿，有限矿产、占领、摧毁易手、重建、人口维护、满仓/尾矿和应急许可证的规则事务闭环均已通过。真实玩家建造入口随阶段 5 UI 接入；`pnpm arena` 仍仅有阶段一已确认的 2 项基线数值断言失败，未新增失败项，因此暂不签核全局阶段门。）

依赖：阶段 2；视觉联调依赖阶段 3。

目标：完成“1000 金币 → 建矿 → 有限产出 → 抢矿”的经济闭环。

工作项：

1. 引入 MinePitDefinition 和 MinePitState。
2. 初始化双方安全矿与四座中立矿的控制权。
3. 实现矿坑中心/周边 1 格的先到先占状态机；同场时现控制方优先，守方清空后换方。
4. 实现金矿专用放置验证和一坑一矿。
5. 实现 6 秒工地、400 扣款、每 4 秒最多开采 100 原矿和 3000 原矿储量。
6. 实现 0–50/51–80/81–100 对应 100%/80%/60% 的纯维护结算函数。
7. 从 reserve 扣 gross，分别记录 upkeep 和 net；钱包达到 5000 时完全暂停。
8. 摧毁、重建、易手和拆除均保留 pit remainingOre。
9. 枯竭后停止产出并提供明确状态。
10. Sandbox 禁用被动收入与自然衰减；旧模式保持原行为。
11. 实现并通过确定性经济/软锁场景验证一次性应急矿许可证，完成最终二选一决策；实际交互文案随阶段 5/8 联调。
12. 建立金币与矿产事件账本，记录人口快照、倍率、gross、upkeep、net、钱包和矿量前后值。

关键自动测试：

- 开局 1000；空等 10 分钟仍为 1000。
- 建矿扣 400；完工前不产出。
- 0、50、51、80、81、100 人口分别得到 100%、100%、80%、80%、60%、60%，101 无法生产。
- 无上限干扰时每坑 30 次完整 gross 开采后恰好归零。
- 全程处于三档时，单坑净入账分别恰好为 3000、2400、1800，维护截留为 0、600、1200。
- 每次结算都满足 gross = upkeep + net，且 reserveConsumed = gross。
- 满仓 10 分钟储量不变；花费后恢复产出。
- 钱包接近上限和矿坑尾数采用比例节流，不浪费原矿且没有浮点漂移。
- 同一时间戳在 81 人死亡 1 人后再结算，本次净收入为 80。
- 队列预留人口不改变倍率；训练完成后即使出口被堵，也在下一次结算进入 used。
- 矿被毁、重建或换方后剩余量不重置。
- 普通格不能建矿，矿坑不能建其他建筑。
- 一格占领范围、先到者同场优先、守方清空换方和敌方金矿阻挡逐项测试。
- 建筑静置 30 分钟生命值不变，受攻击仍可摧毁。
- 玩家与 AI 使用相同的应急规则。

阶段门：

- 可以仅靠矿产完成持续消费，且没有任何被动金币。
- 可在双方镜像位置完成安全矿和中立矿占领。
- 不存在重复产出、负储量、满仓吞矿、维护账本不守恒或摧毁刷新储量。
- 旧模式经济与自然衰减测试全绿。

建议提交：feat(sandbox): add finite mine economy

### 阶段 5：建造、科技与付费生产队列

状态：[~]（实现完成；`pnpm check` 全绿，共 99 个测试文件、647 项测试通过。玩家已可通过专用沙盒面板完成建矿、兵营、基础兵、高级建筑和高级兵的付费生产闭环；权威建造预览、一次性放置选择、锁定建筑灰态、四格出口、科技前置、多栋建筑并行 FIFO 队列、四类出兵建筑独立轮廓、人口预留、ready-blocked、集结点和摧毁清队均已接入主循环。`pnpm arena` 仍仅有阶段一已确认的 2 项基线数值断言失败，未新增失败项，因此暂不签核全局阶段门。）

依赖：阶段 4。

目标：完全替换沙盒中的“直接部署”和“兵营免费自动出兵”。

工作项：

1. 集中定义沙盒建筑目录、种族表现目录和首版平衡值。
2. 实现普通建筑放置事务和工地状态。
3. 检查建造区、道路、保护圈、占用、核心区和塔上限。
4. 生成并预览四格生产出口扇形。
5. 实现兵营、靶场、法师塔、攻城工坊和守卫塔。
6. 实现科技前置和“前置被毁后存量继续、增量禁止”语义。
7. 实现每建筑 5 项 FIFO 单兵队列、入队扣款、训练进度和 ready-blocked。
8. 实现每方 100 人口硬上限、used/reserved 转移和入队人口预留。
9. 实现生产建筑集结点；新兵默认 idle。
10. Sandbox 禁止左侧部署栏直接投放单位。
11. AI 和玩家均只能通过 enqueueProduction 逐个生成单位。
12. 成功放置一栋建筑后退出放置工具；未满足前置的建筑保持灰色禁用；同类生产建筑拥有独立队列并行训练。
13. 复用现有 KayKit 资源，为兵营、靶场、法师塔和攻城工坊提供不同主体模型与场院细节；双方阵营和亡灵表现均保持可辨识映射。

关键自动测试：

- 所有合法/非法建造原因都有稳定 reason code。
- 兵营只训练基础兵，高级建筑只训练对应兵种。
- 无兵营不能开工高级建筑。
- 兵营被毁后，已有高级建筑继续工作但不能再新建。
- 五种单位的费用、时间、人口逐项断言，且每个订单只生成一个实体。
- 队列严格 FIFO、最大 5、只扣款一次。
- used + reserved + 新订单人口超过 100 时拒绝入队，任何时刻都不超出 100。
- 50 人排队 1 人时 committed 为 51，但训练完成前矿收仍为 100%；完成后切换为 80%。
- 80 人完成 1 人单位后切换为 60%；99 人可排队 1 人口单位，但不能排队 2 或 4 人口单位。
- ready-blocked 成品计入 used；解除出口阻塞并生成实体时不得重复计算人口。
- 生产建筑被摧毁时队列只清理一次，分别释放 reserved/used，并按已锁定策略处理退款。
- 出口堵塞时不丢兵、不跳队、不重复扣款；解堵后按序生成。
- 人类与亡灵使用相同规则槽位。
- 成功放置后建筑按钮不再保持选中；未建成兵营时四种高级建筑不可选，兵营建成后同步解锁。
- 两栋同类生产建筑在相同训练时间内各完成一个单位，吞吐量按建筑数并行增长。

阶段门：

- 玩家能完成“矿 → 兵营 → 基础兵 → 高级建筑 → 高级兵”的闭环。
- 所有建筑只占合法锚点，不封道路、矿口或城门。
- 新兵从建筑出口出现，默认不冲锋。
- 现有模式的部署栏与原兵营自动生成仍按旧规则工作。

建议提交：feat(sandbox): add tech tree and production queues

### 阶段 6：单位选择与手动命令

状态：[~]（已改为单单位选择与控制；点击战场目标点后索敌前进，沿路遇敌先战斗、结束后续行。沙盒生产单位均使用一对一控制 ID，不再共享兵团选择。）

依赖：阶段 2 和阶段 5。

目标：把每个沙盒单位的控制权交给玩家，同时保留现有模式自动战斗。

工作项：

1. 引入按单单位控制 ID 工作的 SelectionState。
2. BattlefieldCanvas 和 sceneInteractionBridge 接入单位/建筑 picking。
3. 实现互斥交互状态机：
   neutral、box-selecting、placing-building、attack-move-armed、camera-dragging。
4. 实现单选、框选和 Shift 增删；禁止选择或命令敌军。
5. 实现单位订单和统一下令事务；底层兼容字段必须保持一单位一 ID。
6. 分离持久战略命令与临时局部战斗目标。
7. 实现 move、attack、attack-move、stop、hold。
8. 沙盒关闭自动全选、自动冲城和只准向前的 clampToForwardProgress。
9. 城堡和防御塔继续自动攻击；单位保留动画、冷却、投射物和自动索敌。
10. 多选移动使用可重复的编队目标，不让所有单位争同一格。
11. 增加命令目标标记、无路径/非法目标反馈。
12. 增加移动端命令栏和输入优先级。
13. 处理 pointer cancel、窗口失焦、模式切换、重开和结算时的交互状态清理。
14. 参考历史提交 d5a0754 移植有用概念，不直接恢复旧状态模型。

关键自动与交互测试：

- 单击、框选、Shift 增删结果准确；死亡单位从选择中清理。
- UI 点击不会穿透成战场命令。
- 五种命令覆盖目标死亡、无路径、建筑目标、减员和重新下令。
- stop 后不沿旧路径移动；hold 不离开锚点追击。
- attack-move 结束局部交战后继续原目的地。
- 可向己方后方撤退。
- 无输入运行 60 秒，新兵保持 idle；旧模式仍自动冲城。
- 建造预览、A 键待定和相机拖动不会同时生效。
- 35 个行军时间组合全部在目标 ±8% 内。

阶段门：

- 玩家能连续 10 分钟在三路和换线带控制多个独立单位，不丢命令、不永久卡死、不穿越不可行走格。
- 局部自动战斗不会覆盖玩家战略命令。
- 桌面和移动端都存在完整的下令路径。
- 旧模式 automatic-combat 回归全绿。

建议提交：feat(sandbox): add independent unit commands

### 阶段 7：沙盒敌方 AI

状态：[~]（敌方使用与玩家相同的建造、逐兵生产、集结和单位命令事务，具备可序列化的 Opening/Economy/Tech/Produce/Rally/Defend/Attack/Recovery 状态机、稳定候选排序、三路分波、失败退避、矿枯竭替换、应急矿和 50/80 人口策略。）

依赖：阶段 4、5、6。

目标：让敌方成为遵守同一规则的确定性对手。

阶段机：

    Opening
      → Economy
      → Tech
      → Produce
      → Rally/Defend
      → Attack
      ↘ Recovery/Rebuild ↗

工作项：

1. AI 只调用玩家可用的建造、占矿、生产和命令事务。
2. 候选矿坑、建造格和攻击目标先按距离、再按坐标/ID 稳定排序。
3. 实现安全开局、第二矿、科技升级、集结阈值和三路进攻。
4. AI 识别 50/80 两个软上限，扩军、抢矿和生产配比可以考虑维护档位，但不得修改倍率。
5. 受袭时可从 Rally/Attack 转入 Defend；威胁解除后恢复原目标。
6. 矿毁、枯竭、人口满、队列满、出口阻塞、缺前置和无合法建筑格时采用冷却重试或替代目标。
7. 禁止每帧重复提交同一失败事务。
8. AI 的金币、矿量、队列、人口和命令写入同一事件账本。
9. 首版只做一套可复现的正常难度；难度差异留到平衡阶段。

关键测试：

- 相同初始状态和固定种子运行 20–30 分钟，关键事件序列一致。
- AI 能合法完成建矿、兵营、基础兵、高级建筑、集结和进攻。
- 每次 AI 原矿开采都可追溯为 net 或 upkeep，每枚 AI 金币和每个单位均可追溯。
- AI 不在道路、保护圈或中立普通区域建造。
- AI 不跳前置、训练时间、100 人口上限、维护档位、矿量或出口阻塞。
- AI 在 50/51 与 80/81 附近不会因决策条件相互冲突而持续抖动。
- 钱包满、矿毁、矿枯竭和应急矿规则都与玩家对称。
- 结算后 AI 立即停止决策。

阶段门：

- AI 不使用现有免费部署入口。
- 至少三场 30 分钟灰盒对局中，AI 均能形成经济、科技和有效进攻。
- 异常状态不会形成高频失败循环或无限状态抖动。

建议提交：feat(sandbox): add rule-bound opponent AI

### 阶段 8：胜负、僵局与完整沙盒 UI

状态：[~]（沙盒已具备完整资源/人口 HUD、矿场收益与回本提示、单兵生产预测、单位状态、结算原因、保守资源僵局判定、结算后规则冻结、中性平局表现、重开/退出与完整输入清理。）

依赖：阶段 3、4、5、6、7。

目标：将内部可玩垂直切片变成用户可进入、可理解、可结算的完整模式。

工作项：

1. 在 GameModeSelector 正式展示沙盒模式。
2. 顶栏显示金币/上限、矿坑总原矿、used（+reserved）/100 人口和“无时间限制”。
3. 建造面板显示费用、时间、前置与锁定原因。
4. 生产建筑面板显示 5 格单兵队列、进度、ready-blocked 和集结点。
5. 单位面板显示选择数量、当前命令和“点击目标点索敌前进”提示。
6. 小地图显示八坑的中立/我方控制/敌方控制/有矿/枯竭状态。
7. 常驻显示“军备维护 0%/20%/40%”和“矿产收入 100%/80%/60%”，并提示下一档阈值。
8. 生产按钮预告该订单完成后的 used/committed 人口及可能触发的收入降档。
9. 金矿面板显示本档“原矿 100 → 维护 -20/-40 → 净收入 80/60”，以及按当前档位估算的剩余净价值。
10. 当剩余原矿按当前档位的最大净收入低于 400 建矿成本时显示回本警告，但不禁止建造；三档回本原矿线分别为 400、500、667。
11. 非法放置、无路径、金币不足、缺前置和人口满提供明确反馈。
12. 实现城堡摧毁、同帧双毁和资源僵局三种结算。
13. 结算后冻结所有规则事务，并清除待定输入状态。
14. 平局使用中性文案和音效；不能沿用“非玩家胜利即失败”的旧判断。
15. 提供重新开始与退出；首版不提供胜利后继续。

交互状态清理清单：

- selectedDeployable。
- 单位与建筑选择。
- 框选起点和拖框。
- 建造预览与旋转状态。
- attack-move 待定状态。
- 命令标记和集结点预览。
- 小地图拖动状态。
- 场景交互桥和相机 store。

阶段门：

- 用户无需开发工具即可从主菜单完成整个沙盒闭环。
- 运行超过 300 秒不会触发旧模式超时。
- 单方城堡毁灭、同帧双毁和资源僵局返回正确 resolutionReason。
- 仍有单位、队列、在建生产建筑、足够金币或未枯竭矿坑时绝不误判僵局。
- 结算后金币、矿量、生产进度和 AI 决策不再变化。
- 桌面和 390 × 844 移动视口完成布局验收。

建议提交：feat(ui): expose complete sandbox mode

### 阶段 9：灰盒试玩、平衡与性能硬化

状态：[~]（自动化硬化完成：3 场 30 分钟确定性耐久场、50/80 人口经济预算、30/38 步风险收益、塔与核心区防龟、100/200 实体模拟与浏览器基准、导航/小地图/内存检查、旧模式回归均已通过；`pnpm check` 为 104 文件/674 测试全绿，`pnpm arena` 为 3 文件/5 场景全绿。仍需补做 3 场真人完整灰盒试玩，并补录阶段 0 未留下的旧 80 单位浏览器数字后才能签核。）

依赖：阶段 8。

目标：验证真实节奏，不用修改地图尺寸来掩盖系统问题。

工作项：

1. 完成至少 3 场、每场至少 30 分钟的完整灰盒对局。
2. 至少一场使用双方镜像起点验证。
3. 记录首次建矿、首次基础兵、首次高级兵、首次接敌、首次中立矿争夺和结束时间。
4. 校准生产建筑成本/时间/生命值、守卫塔数值和 AI 集结阈值。
5. 记录 50→51 与 80→81 的扩军决策，验证维护断崖是否清楚且可接受。
6. 验证安全矿支持 0–50 高效经济、中立矿支持 51–80 扩军、81–100 需要更大地图控制的预期。
7. 验证中央 30 步和侧路 38 步是否形成清晰风险收益。
8. 检查塔最多 2 座和核心区限制能否防止永久龟缩。
9. 跑 100 个总人口的代表性战斗和双方各 100 人口的 200 单位压力测试。
10. 对 871 格渲染、导航缓存、动态阻挡和小地图更新进行 profiling。
11. 检查长局内存、事件队列和对象池。
12. 对旧模式执行完整回归。

参考性能预算：

- 1000 次随机可达寻路：预热后 p95 ≤ 3ms，单次最大值 ≤ 10ms，成功率 100%。
- 100 个活跃单位持续战斗 5 分钟：模拟步 p95 ≤ 8ms，单帧不超过 50ms；参考开发机平均 ≥ 55 FPS，1% low ≥ 45 FPS。
- 双方各 100 人口、最多约 200 个作战实体持续 5 分钟：模拟步 p95 ≤ 16ms，任何模拟步不超过 50ms；参考开发机平均 ≥ 45 FPS，1% low ≥ 30 FPS。
- 预热后内存增长小于 10MB/分钟。
- 当前 80 单位 benchmark 不比阶段 0 基线慢超过 20%。
- 地形 draw call 不得随 871 个格子近似线性增长。

性能测试放在受控目标机或独立任务中，普通 CI 只检查确定性的功能结果，避免把机器抖动误判为代码回归。

阶段门：

- 三场完整对局都能走通经济、科技、手动指挥和结算。
- 无非预期经济软锁、永久单位卡死、出口吞兵或资源泄漏。
- 行军时间、帧预算和旧 benchmark 均达标。
- pnpm check 和 pnpm arena 全绿。

建议提交：perf(sandbox): harden large battle simulation

### 阶段 10：灰盒确认后的美术与音频

状态：[x]（官方六边地块、123 格三路中心线、八个矿山岩脊、双方农田、东侧浅水河道/桥梁/河岸森林、环境动物与阶段反馈音均已接入；规则拓扑保持不变。）

依赖：阶段 9，并需明确确认灰盒拓扑。

目标：在不改变规则地图的前提下，把三路和矿区做出清晰视觉差异。

工作项：

1. 中路使用开阔、低遮挡的主战场语言。
2. 西路使用矿山、矿车、矿渣和岩壁视觉模块。
3. 东路使用森林、河岸和桥梁视觉模块，但桥梁不得成为单格必经点。
4. 玩家、中立、敌方矿坑使用一致轮廓与不同状态表现。
5. 为可建区、道路、矿坑保护圈和生产出口保留清晰视觉提示。
6. 复用现有项目原生 3D 资产；新资产单独记录来源与运行时校验。
7. 建造、完工、队列完成、出口阻塞、占矿、枯竭和命令确认接入音效。
8. 所有装饰物默认为纯视觉；需要碰撞时必须在地图逻辑层显式登记并重新跑拓扑测试。

实施记录：

- 主地块、农田、水域、桥梁与植被继续优先使用 KayKit Medieval Hexagon Pack；场景新增 78 个纯视觉装饰，总场景元素为 174 个。
- 从 Quaternius Ultimate Animated Animals 选用自带动画的牛、鹿、狐狸共 5 个环境实例；不参与选中、碰撞、索敌或寻路。
- 复用现有本地 UI 音频，为建造开始/完工、队列完成、出口阻塞、占矿、矿坑枯竭和命令确认增加状态变化反馈。
- 自动验收为 107 个测试文件、694 个测试全绿；资源校验、生产构建与浏览器截图回归通过，复核期间无新增控制台警告或错误。

阶段门：

- 视觉层不改变 871/331/8/34/30/38/6/12 等锁定数字；需要碰撞的矿山格已作为显式规则变更登记。
- 三路在正常相机高度下可快速辨识。
- 八坑入口和剩余状态不会被模型遮挡。
- 资产检查、场景检查、截图回归和完整测试全绿。

建议提交：feat(scene): dress sandbox large battlefield

---

## 6. 测试矩阵

### 6.1 单元与属性测试

建议新增或拆分：

- tests/map/sandboxLargeDefinition.test.ts
- tests/map/sandboxLargeConnectivity.test.ts
- tests/game/sandboxEconomy.test.ts
- tests/game/populationUpkeep.test.ts
- tests/game/minePits.test.ts
- tests/game/construction.test.ts
- tests/game/productionQueues.test.ts
- tests/game/squadOrders.test.ts
- tests/game/sandboxOpponentAi.test.ts
- tests/game/sandboxVictory.test.ts
- tests/scene/sandboxCamera.test.ts
- tests/scene/minimapProjection.test.ts
- tests/ui/sandboxControls.test.ts

所有锁定数字都应由纯数据测试覆盖，不依赖 Three.js 渲染完成后才发现错误。

### 6.2 集成场景

至少维护以下确定性场景：

1. 开局静置 10 分钟，无被动金币。
2. 0–50 人口下，单矿从建造到枯竭完成 30 次 gross 开采并净入账 3000。
3. 51–80 人口恒定开采单坑，净入账 2400、维护 600。
4. 81–100 人口恒定开采单坑，净入账 1800、维护 1200。
5. 50/51、80/81 跨档、死亡降档和队列完成升档。
6. 满仓暂停、近满仓比例节流后消费并恢复产出。
7. 中立矿先到者在双方同场时保持控制；先到方被清空后由另一方接管。
8. 金矿摧毁、换方、重建后原矿储量连续。
9. 兵营 → 高级建筑 → 高级兵完整科技链。
10. 队列满、100 人口满、预留释放和出口堵塞恢复。
11. 多选单位中路推进、侧路绕行和联络带换线。
12. AI 合法开局、维护档位与矿毁恢复。
13. 城堡胜负、同帧双毁和资源僵局。
14. legacy 战局结束后立即启动 sandbox，再反向启动，验证无串局。

### 6.3 手工可玩性验收

每场记录：

- 首矿开工/完工时间。
- 首支基础兵和高级兵时间。
- 首次接敌的路线与时间。
- 首次换线是否容易理解。
- 中立矿争夺是否给玩家足够反应时间。
- 跨过 50/80 人口前是否能清楚预见收入变化，跨档后是否能理解维护去向。
- 81–100 人口是否确实需要额外矿区，而不是成为无意义或永远到不了的区间。
- 投石车侧路时间是否过长。
- 是否出现一座塔或一栋建筑封死路线。
- 无收入时是否清楚知道下一步。
- 队列、人口、前置和矿坑状态是否无需猜测。
- 胜负或平局原因是否明确。

### 6.4 旧模式回归

逐项保护：

- legacy 地图格数、地形、结构和相机范围。
- 初始金币、金币上限和被动恢复。
- 建筑自然衰减。
- 部署栏直接投兵和现有拖放。
- 自动选中、自动冲锋、城堡锁定和前进限制。
- campaign/normal/arena 的计时、奖励和超时结算。
- 现有 AI 难度与部署节奏。
- 桌面和移动端布局。

---

## 7. 风险与控制

| 风险 | 影响 | 控制方式 |
|---|---|---|
| 全局 BATTLEFIELD_MAP 单例 | 新旧地图串局或场景仍读旧地图 | 阶段 1 先注入定义，保留 legacy adapter，加入同进程隔离测试 |
| 规则散落在 UI | AI 与玩家规则不一致 | 所有动作走统一事务和 reason code |
| 871 格寻路线性扫描 | 多单位长局卡顿 | 坐标索引、邻接缓存、优先队列、阶段 0 基线与阶段 9 profiling |
| 自动战斗覆盖手动命令 | 玩家目标失效或单位自动冲城 | 持久目标点与临时目标分离，sandbox controlAuthority 独立 |
| 动态建筑封路 | 单位、AI 或生产出口永久卡死 | 331 保留格禁建、出口扇形、放置连通性检查 |
| 有限经济软锁 | 玩家无法继续游戏 | 一次性应急矿或最后建矿资金规则，发布前固化一种 |
| 满仓消耗矿量 | 有限资源被静默浪费 | 先按钱包净容量反推 gross；满仓时 gross 为 0，近满仓时比例节流 |
| 摧毁金矿刷新储量 | 无限刷矿 | 储量归 MinePitState，不归 BuildingState |
| 人口阈值 off-by-one 或同帧顺序不一 | 敌我收入不同、重放不确定 | 固定 0–50/51–80/81–100，并锁定伤亡/完工/采矿顺序 |
| 队列和出口成品人口重复计算 | 错误降档或突破 100 | used/reserved 单向转移、队列/出兵集成测试 |
| 维护费与原矿记账不守恒 | 凭空丢矿或多发金币 | 每次验证 gross = upkeep + net 和 reserveConsumed = gross |
| 高人口过快燃烧有限矿藏 | 扩军后经济软锁 | 回本提示、应急矿、长局经济模拟和 50/80 阈值试玩 |
| 双方满人口约 200 个实体 | 大地图战斗帧率不足 | 100 单位性能门与 200 单位压力 soak 分开验收 |
| AI 作弊或高频失败 | 破坏公平、浪费 CPU | 同接口、事件账本、稳定排序、失败重试冷却 |
| 移动端框选与相机冲突 | 无法稳定下令 | 显式交互状态机；移动端使用命令栏和目标模式 |
| 美术 props 改变拓扑 | 灰盒验收失效 | 逻辑碰撞与视觉分层，任何碰撞变更重跑几何属性测试 |
| 僵局误判 | 提前结束仍可继续的战局 | 保守条件、事件触发检查、完整可恢复能力测试 |

---

## 8. MVP 不包含

以下内容明确延期，避免沙盒首版失控：

- 工人采集与工人寻路。
- 维修、自动回血和建筑升级。
- 住房、扩容建筑和复杂人口科技；固定 100 上限与三档维护属于 MVP。
- 市场、贸易、资源转换和第二种资源。
- 战争迷雾与侦察系统。
- 自由城墙、城门编辑和任意领土扩张。
- 单位技能树、科研升级和装备系统。
- 编队快捷键、巡逻、Shift 队列命令。
- 从小地图直接下达部队命令。
- 多种沙盒 AI 难度。
- 网络多人、存档后端和回放系统。
- 胜利后继续经营。

这些能力可以在 Sandbox v1 稳定后另立计划，不能作为本计划阶段完成的隐含条件。

---

## 9. Definition of Done

沙盒模式只有同时满足以下条件才算完成：

- 871 格大地图、331 保留格、8 坑、每方 34 建造锚点和 8 × 8 矿山阻挡格全部由自动测试锁定。
- 三路 30/38/38 步，换线 6/12 步，实际行军时间全部在 ±8% 内。
- 开局 1000、无被动收入、5000 上限、每坑 3000 原矿和满仓暂停行为正确。
- 每方 100 人口硬上限；used/reserved 预留、完成、死亡和出口阻塞转移正确。
- 0–50/51–80/81–100 的矿产倍率为 100%/80%/60%，gross/upkeep/net 账本始终守恒。
- 建筑不会自然掉血，但能正常受伤、摧毁和结算。
- 金矿只能建在矿坑，储量跨摧毁和易手保持。
- 基础与高级兵只能从正确生产建筑通过付费队列生成。
- 队列、人口预留、前置和出口阻塞没有丢兵、重复扣款、重复计人口或跳队。
- 玩家可以稳定完成选择、移动、攻击、攻击移动、停止和坚守。
- 新兵默认 idle，沙盒没有自动冲城或禁止后撤。
- AI 与玩家使用同一经济、建造、生产和命令接口，没有免费资源。
- 城堡胜负、同帧双毁和资源僵局正确，结算后规则状态冻结。
- 小地图、相机、桌面和移动端 UI 完成验收。
- 30 分钟完整对局、100 单位性能场景和 200 单位满人口压力 soak 无崩溃、无永久卡死、无资源泄漏。
- 性能预算达标；旧 80 单位 benchmark 回归不超过 20%。
- campaign、normal、arena 和内部 infinite 的现有规则及 UI 回归全绿。
- pnpm check、pnpm arena、资产校验和关键截图回归全部通过。
- 灰盒验收通过后才进入美术阶段；美术不得改变已经锁定的逻辑拓扑。

---

## 10. 推荐执行节奏

按风险而不是按画面完整度推进：

    阶段 0 基线
      → 阶段 1 模式/地图注入
      → 阶段 2 大地图灰盒与导航
      → 阶段 3 相机/小地图
      → 阶段 4 有限矿产
      → 阶段 5 建造/科技/生产
      → 阶段 6 手动命令
      → 阶段 7 同规则 AI
      → 阶段 8 完整 UI/结算
      → 阶段 9 试玩/平衡/性能
      → 阶段 10 美术/音频

前三个阶段先证明“大地图可以被正确注入、遍历、观察和寻路”；中间四个阶段完成 RTS 规则闭环；最后三个阶段负责产品化、长局稳定性和表现。这样每个提交都能独立回归，也能在灰盒或经济验证不通过时停下，而不会把未经验证的地图、规则和美术一次性绑在一起。
