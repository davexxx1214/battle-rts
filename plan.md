# Battlefield RTS 自动部署玩法开发计划

状态：阶段 0、1、2、3、4、5、6、7、8 已完成，阶段 9 数值调优与整体验收进行中
目标版本：Gold Deployment Vertical Slice 0.3
开发分支：`dev`
当前基线：部署式自动战斗闭环、双桥河流战场、金币和建筑事件表现、对手部署 AI、职业单位、投射物、音效和性能基准已经可用

## 这轮要做成什么

这一阶段要把当前可手动选兵和下命令的 RTS 原型，改成接近《皇室战争》的部署式自动战斗。玩家管理金币，在己方势力范围内放置建筑和士兵；士兵生成后不再接受任何操作，会沿路径冲向敌方城堡，清理路上的敌军和建筑。城堡被攻破时立即结束比赛。

这一版先完成一局可玩的闭环：金币增长，部署单位或建筑，金矿生产金币，兵营生产剑士，双方自动交战，城堡激活反击，最后结算胜负。卡组循环、局外成长和多人联机不在本轮范围内。

## 成功标准

- 双方开局都有 500 金币，金币上限为 1000。
- 第 1 至 4 分钟每 2.8 秒增加 100 金币，第 5 分钟每 1.4 秒增加 100 金币。
- 金币达到 1000 后不再增长，界面会给出持续可见且不会反复打扰的满额提示。
- 屏幕左侧陈列当前全部可部署兵种和建筑，费用、可用状态和不可用原因清楚可见。
- 建筑只能占据己方势力范围内一个完整、空闲、可建造的六边形格子。
- 没有有效格子时不能放置建筑，建筑按钮显示不可用原因。
- 金矿费用为 700，寿命为 36 秒；被提前摧毁时立即停止生产。
- 金矿部署 4 秒后首次产出金币，之后每 4 秒增加 100 金币，完整存活共生产 900 金币。
- 兵营费用为 700，寿命为 30 秒；部署 5 秒后生成第一名剑士，之后每 10 秒生成一名，完整存活共生成三名。
- 每个阵营同时最多存在 1 座金矿和 2 座兵营。
- 玩家不能选择、移动、集火、停止或命令士兵，原有主动后撤和败退行为全部取消。
- 士兵在抵达城堡前，会优先攻击前进路径上遇到的敌方兵种和建筑。
- 士兵第一次开始攻击城堡后永久锁定城堡，不会因为受到攻击或出现新目标而转移。
- 士兵可以停下攻击和横向绕路，但任何移动都不能让它沿进攻方向后退。
- 城堡开局不攻击，第一次受到敌方伤害后永久激活；城堡被摧毁时所属方立即失败。
- 玩家和敌方 AI 遵守同一套金币、费用、部署区域、格子占用和建筑数量限制。
- 五分钟结束时比较双方城堡剩余生命，生命值较高的一方获胜；生命值完全相同时判为平局。
- 固定初始状态和输入运行两次，金币变化、部署结果、生成顺序、目标选择和胜负一致。
- 所有平衡数值都能通过统一规则配置调整，不需要修改 UI、场景组件或战斗流程代码。
- `pnpm check` 全部通过，浏览器控制台没有 warning 或 error。

## 已确认的玩法规则

### 金币与比赛阶段

| 规则 | 数值 |
| --- | ---: |
| 初始金币 | 500 |
| 金币上限 | 1000 |
| 第 1 至 4 分钟产出 | 每 2.8 秒增加 100 |
| 第 5 分钟产出 | 每 1.4 秒增加 100 |
| 溢出处理 | 达到上限后直接丢弃新增金币 |

双倍阶段只加快双方的被动金币恢复，不加快金矿生产、兵营出兵、建筑自然掉血或攻击冷却。金币满额提示只在从未满变为满额时播放一次声音和动画，之后保持高亮；花费金币后提示立即解除，再次达到 1000 时可以重新触发。

### 部署费用

所有费用都是 100 的倍数，单次部署费用必须在 100 到 1000 之间。下面是本轮实现基线，战斗调优阶段可以修改集中配置，不把数值散落到组件和战斗循环里。

| 项目 | 费用 | 单次编制 | 本轮约定 |
| --- | ---: | ---: | --- |
| 剑士 | 400 | 3 | 兵团购买基线 |
| 弓箭手 | 300 | 2 | 兵团购买基线 |
| 法师 | 600 | 2 | 兵团购买基线 |
| 投石车 | 800 | 1 | 单台攻城单位 |
| 金矿 | 700 | 1 | 已确认 |
| 兵营 | 700 | 1 | 已确认 |

城堡属于初始固定设施，不进入部署栏，也没有部署费用。

直接部署的作战单位按兵团出售，一次点击只扣除一次费用并生成一个固定编制 squad。兵营按生产周期生成的剑士不属于直接购买，仍然每次生成一名。

### 数值平衡原则

本计划中的数值都是首个可玩版本的实现基线，不是最终固定值。表格中的“已确认”只表示本轮按该数值开发和验收；完成可玩闭环后，还要根据实际对局持续调整平衡性。

以下参数必须作为可调配置集中管理：

- 建筑：费用、生命值、防御力、自然掉血速度、寿命、数量限制和爆炸效果。
- 金矿：首次产出时间、产金币间隔、单次产量和完整寿命总产量。
- 兵营：首次出兵时间、后续出兵间隔、出兵数量和生成兵种。
- 兵种：费用、生命值、攻击力、防御力、攻击速度、攻击距离、移动速度、索敌距离和碰撞属性。
- 城堡：生命值、攻击力、防御力、攻击速度、攻击距离和激活后的索敌参数。
- 经济：初始金币、金币上限、被动产出间隔、单次产量和不同比赛阶段的倍率。

完整格子占用、只能在己方势力范围部署、士兵不可操控、攻击城堡后永久锁定目标、五分钟按城堡剩余生命判定胜负等属于玩法机制。它们不随普通数值调优自动改变，如需修改必须单独做设计决策。

### 势力范围和六边形格子

- 双方开局只能在各自河岸一侧的势力范围内部署。
- 河流、桥梁、城堡、城门入口、装饰障碍和势力范围外的格子不可建造；己方领地内其余空地均可建造。
- 建筑必须放在完整六边形格子上，每个建筑占据一个格子。
- 同一格子最多存在一个建筑；建筑不能覆盖存活单位，士兵也不能在建筑占用格内部生成。
- 建筑完成爆炸并从模拟中移除后，格子重新变为可用。
- 建筑允许占据原行军通道；进攻单位遇到挡路的敌方建筑时优先攻击建筑，摧毁后继续向城堡推进。
- 选中建筑后显示六边形预览，可建造时为绿色，不可建造时为红色。
- 只有成功放置才扣除金币。取消选择、点到无效格子或格子在点击前被占用都不扣金币。

本轮采用固定势力范围，不做摧毁前哨后扩张部署区域。势力范围扩张留到后续版本。

### 建筑生命周期

- 玩家放置的金矿和兵营会随时间线性掉血，生命归零时爆炸并移除。
- 敌方攻击造成的伤害会缩短建筑实际存活时间。
- 建筑在生命大于零时保持完整功能，不增加残血停工阶段。
- 城堡不受自然掉血影响。
- 首轮实现基线为金矿 900 生命、兵营 1200 生命、销毁表现期 0.8 秒；这些数值与其他平衡参数一样集中配置，后续可以直接调优。
- 建筑死亡、自然到期和单位攻击共用同一套伤害与销毁事件，渲染层不自行计算寿命。
- 每个阵营同时最多存在 1 座金矿和 2 座兵营。建筑死亡并移除后，对应名额立即恢复。

金矿费用为 700，寿命为 36 秒。部署 4 秒后首次生产 100 金币，之后每 4 秒生产 100，完整存活产出 900。最后一次产出发生在第 36 秒，随后自然爆炸。

兵营费用为 700，寿命为 30 秒，只生成剑士。部署 5 秒后生成第一名，之后每 10 秒生成一名，在第 5、15、25 秒共生成三名剑士，不设置死亡出兵。

### 士兵行为

士兵只有三个战斗状态：冲锋、沿途交战和城堡锁定。

冲锋时，士兵以敌方城堡为最终目的地，沿可通行路径前进。如果前进路径上出现可以攻击的敌方士兵或建筑，会停下并进入沿途交战。多个目标同时出现时选择距离最近的目标，距离相同则用稳定 ID 决定，保证模拟结果可重复。

沿途交战时，士兵攻击当前目标。目标死亡或失效后重新检查前方路径，有其他目标就继续攻击，没有目标就恢复冲锋。士兵不会回头追逐已经落到身后的敌人。

士兵第一次对城堡进入攻击动作时切换到城堡锁定。锁定后不再进行普通索敌，即使受到攻击、附近出现新单位或新建筑，也只会继续接近并攻击城堡，直到自己死亡或城堡被摧毁。远程兵种在城堡进入射程后同样遵守这条规则。

所有士兵都可以为绕开障碍做横向移动，也可以停下攻击。移动速度在进攻轴上的分量不能为负，弓箭手、法师和投石车被近身后原地攻击，不再后退保持射程。

### 城堡和胜负

- 双方各有一座固定城堡，城堡有生命值、攻击范围、攻击力和攻击冷却。
- 城堡开局处于休眠状态，不主动攻击。
- 城堡第一次受到大于零的敌方伤害时永久激活，之后自动攻击射程内的敌人。
- 城堡激活状态不会因为脱离战斗而重置。
- 城堡攻击范围不得小于弓箭手和法师，但可以小于投石车。
- 任意一方城堡生命归零时立即结束比赛，另一方获胜。
- 如果双方城堡在同一个固定步内同时归零，本局判为平局。

比赛时间固定为五分钟。时间结束时比较双方城堡剩余生命，生命值较高的一方获胜；生命值完全相同时判为平局，不进入加时赛。

## 本轮不做

- 四张手牌、八张卡组和卡牌循环。
- 手动框选、移动、集火、攻击移动、停止和坚守。
- 士兵主动后撤、保持射程、败退和返回营地。
- 建筑升级、科技树、人口、生产队列和局外养成。
- 摧毁前哨后扩张势力范围。
- 主动技能、法术卡和英雄技能。
- 多人联机、账号、排行榜、存档和后端服务。
- 移动端触控布局，本轮以 1280×720 桌面视口验收。

## 代码调整方向

战斗模拟继续使用纯 TypeScript，React 和 Three.js 只消费状态、发送部署请求并表现事件。金币结算、格子校验、目标选择和胜负不能写在 UI 或场景组件里。

- 新增 `src/game/rules.ts`，集中保存经济、费用、兵种与建筑属性、城堡属性、生产周期、比赛阶段和部署限制。UI、AI 和模拟层必须读取同一份配置，禁止各自复制数值。
- 新增 `src/game/economy.ts`，处理双方金币、恢复进度、倍率切换、上限和满额事件。
- 新增 `src/game/deployment.ts`，处理部署请求、费用校验、六边形格子占用和失败原因。
- 新增 `src/game/buildings.ts`，处理金矿、兵营、城堡、自然掉血、生产和销毁。
- 新增 `src/game/autoCombat.ts`，处理路径目标、普通交战和城堡永久锁定。
- `src/game/battle.ts` 保留固定步长总循环和公开状态，逐步移除手动命令、远程后撤和败退职责。
- `src/game/events.ts` 增加金币满额、部署、建筑受损、建筑死亡、金币产出、兵营出兵、城堡激活和城堡摧毁事件。
- `src/map/battlefield.ts` 增加阵营势力范围、可建造标记、保留行军通道和格子占用需要的稳定键。
- 新增 `src/ui/DeploymentSidebar.tsx` 和 `src/ui/GoldMeter.tsx`，从 `src/App.tsx` 拆出部署栏、金币条和比赛计时。
- 新增 `src/scene/buildings`，放置金矿、兵营和城堡模型及其生命条。
- 新增 `src/scene/placement/PlacementPreview.tsx`，显示有效和无效六边形预览。
- `src/scene/BattlefieldCanvas.tsx` 移除框选和右键命令入口，改为把战场点击转换成部署请求。
- 保留现有投射物、战斗事件、音效对象池和单位模型，删除已经没有入口的撤退表现逻辑。

## 开发阶段

### 阶段 0：冻结规则和回归基线

依赖：无。

工作内容：

- [x] 金矿确定为每 4 秒生产 100 金币，完整存活生产 9 次。
- [x] 兵营确定为 700 金币、30 秒寿命，在第 5、15、25 秒各生成一名剑士。
- [x] 每个阵营同时最多存在 1 座金矿和 2 座兵营。
- [x] 五分钟结束时按双方城堡剩余生命判定胜负，同血时判平局。
- [x] 记录当前 `pnpm check` 结果和一场完整战斗的基线截图。
- [x] 列出需要替换的手动命令、远程后撤和败退测试，已有投射物、伤害和确定性测试继续保留。

验收：

- 本轮基线数值写入 `src/game/rules.ts`，配置按模块分组并进行范围校验；后续平衡调整只修改集中配置。
- 当前分支没有与本阶段无关的未提交改动。

### 阶段 1：加入比赛时钟和金币系统

依赖：阶段 0。

工作内容：

- [x] 为双方状态增加当前金币、恢复进度、是否满额和最近一次满额提示序号。
- [x] 增加五分钟比赛时钟和 1 倍、2 倍金币阶段。
- [x] 用累计进度处理 2.8 秒与 1.4 秒间隔，倍率切换时保留未完成的恢复进度。
- [x] 大步长更新和多个小步长更新必须得到相同金币结果。
- [x] 金币达到 1000 后丢弃被动产出，不保存隐藏溢出值。
- [x] 进入满额时只产生一次事件，花费后再次满额可以产生新事件。
- [x] 重开战斗时恢复 500 金币、五分钟和 1 倍阶段。

测试：

- [x] 开局 2.8 秒后金币从 500 变为 600。
- [x] 前四分钟使用 2.8 秒周期，第五分钟使用 1.4 秒周期。
- [x] 金币永远不会超过 1000。
- [x] 满额期间连续更新不会重复产生满额事件。
- [x] 双方在相同条件下得到相同金币。

### 阶段 2：建立势力范围、建筑格和占用规则

依赖：阶段 1。

工作内容：

- [x] 为地图格子增加所属阵营、是否允许建造和是否属于保留行军通道。
- [x] 把河流、桥梁、城堡格、城门入口和装饰障碍标记为不可建造；己方领地内其余空地保持可建造。
- [x] 增加格子占用表，通过稳定六边形坐标查找建筑。
- [x] 增加世界坐标到六边形格子的点击转换和边界校验。
- [x] 部署前验证阵营、地形、完整格子，以及建筑和存活单位占用。
- [x] 提供某阵营是否还存在有效建筑格的纯函数，供侧栏禁用状态使用。

测试：

- [x] 双方只能在自己的势力范围内建造。
- [x] 河流、桥梁、城堡和已占用格子会拒绝部署。
- [x] 任何成功部署都不会切断所有进攻路线。
- [x] 建筑移除后对应格子重新可用。
- [x] 没有格子时返回稳定的 `no-buildable-hex` 失败原因。

### 阶段 3：加入建筑实体、自然掉血和生产

依赖：阶段 2。

工作内容：

- [x] 新增金矿、兵营和城堡结构类型，保存阵营、格子、生命、最大生命、创建时间和自然寿命。
- [x] 自然掉血与战斗伤害走同一条生命结算链路，生命归零只产生一次死亡事件。
- [x] 金矿按固定周期产出金币，金币满额时本次产出丢失，生产计时继续前进。
- [x] 兵营按固定时间点在相邻有效位置生成剑士，生成位置被占时选择稳定的备选位置。
- [x] 兵营找不到任何生成位置时跳过本次出兵，不把士兵排入隐藏队列。
- [x] 建筑自然到期时先处理同一时刻的生产，再处理死亡，避免最后一跳因更新步长不同而丢失。
- [x] 建筑爆炸完成后释放格子，模拟层先标记死亡，渲染层按事件播放效果。

测试：

- [x] 金矿完整存活的产出次数与配置一致。
- [x] 金矿被提前摧毁后不再产出。
- [x] 第 36 秒的生产和自然死亡顺序固定。
- [x] 兵营只生成己方剑士，生成 ID 和位置可重复。
- [x] 自然掉血与敌方伤害同时发生时不会重复销毁建筑。

### 阶段 4：完成左侧部署栏和战场放置交互

依赖：阶段 1、阶段 2、阶段 3。

工作内容：

- [x] 左侧按兵种和建筑两组陈列金矿、兵营、剑士、弓箭手、法师和投石车。
- [x] 每个项目显示图标、名称、费用和当前状态。
- [x] 金币不足、建筑数量达到上限或没有建筑格时按钮变灰，并显示具体原因。
- [x] 点击项目进入部署模式，鼠标移动时显示位置或六边形格子预览。
- [x] 成功部署后自动取消选中，按 Escape 或右键可以主动取消。
- [x] 费用只在部署请求成功后扣除，重复点击和无效点击不能重复扣费。
- [x] 部署走单一原子事务，同时校验费用、数量限制、格子和路径，并一次性更新金币、建筑实体与占用表；UI 和 AI 不得直接扣款或写占用。
- [x] 移除框选、右键移动、集火、攻击移动、停止和坚守 UI。
- [x] 金币条显示当前值、上限、恢复倍率和下一次恢复进度。
- [x] 金币达到 1000 时金币条闪烁并显示“金币已满，立即部署！”，提示音每次满额只播放一次。

验收：

- 不看说明文字也能完成选中、预览、部署和取消。
- 周期更新不会替换用户正在点击的侧栏 DOM 节点。
- 1280×720 下侧栏不会遮挡主要桥梁和城堡。

### 阶段 5：替换士兵移动和索敌逻辑

依赖：阶段 3。

工作内容：

- [x] 删除玩家命令、远程后撤、保持最小射程、败退和返回营地状态。
- [x] 为单位增加冲锋、沿途交战和城堡锁定三个自动战斗状态。
- [x] 自动路径以敌方城堡为最终目标，索敌同时包含敌方士兵和可攻击建筑。
- [x] 只选择前进路径附近且没有落到身后的目标，多个候选按距离和稳定 ID 排序。
- [x] 普通目标死亡后恢复冲锋或攻击下一个沿途目标。
- [x] 单位第一次对城堡产生攻击开始事件时永久锁定城堡。
- [x] 城堡锁定单位受到伤害、被包围或出现更近目标时仍不重新索敌。
- [x] 远程单位被近身后原地攻击，不能调用后撤路径。
- [x] 绕障移动允许横向修正，但进攻轴速度不能为负。

测试：

- [x] 路径上存在敌兵时先攻击敌兵，再继续前往城堡。
- [x] 路径上存在敌方建筑时先攻击建筑。
- [x] 路径外或身后的单位不会诱使士兵回头。
- [x] 士兵开始攻击城堡后忽略所有新增目标和受到的攻击。
- [x] 远程兵种不会为了拉开距离向本方移动。
- [x] 相同候选目标和位置始终得到相同索敌结果。

### 阶段 6：完成城堡激活、反击和胜负

依赖：阶段 3、阶段 5。

工作内容：

- [x] 把双方城堡加入权威战斗状态和目标查询。
- [x] 城堡开局休眠，第一次收到大于零的敌方伤害时产生激活事件。
- [x] 激活后的城堡自动攻击射程内最近敌人，不再回到休眠状态。
- [x] 城堡不受自然寿命影响，也不占用玩家可建造数量。
- [x] 城堡生命归零时立即锁定胜负，停止新的金币、生产、部署和攻击结算。
- [x] 五分钟结束时，如果双方城堡都未被攻陷，按剩余生命立即结算胜负，同血时判平局。

测试：

- [x] 未受伤城堡不会攻击。
- [x] 第一次受伤只产生一次激活事件。
- [x] 城堡激活后即使附近没有敌人也保持激活。
- [x] 城堡被摧毁的同一固定步内不会继续造成新伤害。
- [x] 双方城堡同一时刻归零时按明确规则结算：平局。

### 阶段 7：让敌方 AI 使用同一套部署系统

依赖：阶段 4、阶段 6。

工作内容：

- [x] 敌方 AI 只读取公开战斗状态，通过与玩家相同的部署接口行动。
- [x] AI 不能跳过费用、建筑数量、势力范围或格子占用校验。
- [x] AI 在经济、兵营和直接出兵之间使用简单且可重复的决策规则。
- [x] 建筑部署从有效格子列表中按稳定评分选择，不依赖浏览器帧率或随机时间。
- [x] AI 没有可执行动作时保存金币，不产生无效部署循环。

测试：

- [x] AI 部署后扣除正确金币。
- [x] AI 不会在玩家区域、河流或已占用格子建造。
- [x] 相同初始状态运行两次会得到相同部署顺序。
- [x] AI 金币不足时不会产生单位或建筑。

首版 AI 每 1 个模拟秒最多决策一次，优先维持 1 座金矿和 1 座兵营，之后按剑士、弓箭手、法师、投石车的固定循环储蓄和部署。该节奏、目标建筑数量和兵种循环均属于可调平衡参数，不是最终固定值。

### 阶段 8：补齐建筑、金币和城堡表现

依赖：阶段 4、阶段 6、阶段 7。

工作内容：

- [x] 复用现有中世纪资源，为双方提供金矿、兵营和城堡模型及阵营颜色。
- [x] 建筑显示简洁生命条，自然掉血和战斗伤害都反映到同一条生命值。
- [x] 修正建筑生命条透明层的渲染顺序，生命值填充不会再被黑色底框覆盖。
- [x] 从已购 KayKit Source 包中精选旗帜、武器架、靶子、矿石、木料和补给道具；按城堡、兵营、金矿职责组合建筑，并补充统一六边形石质基座。
- [x] 校验所有运行时 KayKit `.gltf` 引用的纹理和二进制缓冲文件，避免发布后出现缺失模型。
- [x] 参考 KayKit 官方样例重做地块表现：恢复明亮的原始图集风格，按六方向拓扑自动拼接双方营地到两座桥的道路，并用预组装树林和岩丘丰富不可部署区域。
- [x] 建筑部署、金矿产出、兵营出兵、城堡激活和建筑爆炸使用对应事件驱动动画与音效。
- [x] 金矿每次成功产出显示短促金币反馈，满额浪费时显示不同提示。
- [x] 城堡激活时表现国王进入战斗，不能只靠 HUD 文字说明。
- [x] 胜利和失败面板保留重新开始入口，重开后清空建筑、占用、锁定目标和所有计时器。
- [x] 把中央边界改为连续河流；靠近主战场的左右通道各由原有三格桥面加三格内侧扩展地块组成，补齐两处单格水洞，中央保留连续不可通行水带，不加载额外桥梁模型。
- [x] 使用精选 Kenney CC0 树木、岩石、资源和聚落模型密集填充中央走廊两侧；横向超出中央六格范围的领土地块全部不可通行、不可部署兵种且不可建造，桥梁继续使用原有六边形地块表现。
- [x] 将原始免费素材包保留在本地并忽略，只提交运行时需要的模型和对应许可证。

验收：

- 只看战场可以判断建筑是否在工作、是否即将销毁、兵营何时出兵和城堡是否激活。
- 重开三次后没有残留建筑、重复事件、悬空音频或失效格子。

### 阶段 9：数值调优和整体验收

依赖：前面所有阶段。

自动检查：

- [x] `pnpm lint`
- [x] `pnpm typecheck`
- [x] `pnpm test`
- [x] `pnpm build`
- [x] `pnpm check`
- [x] 同一初始状态运行两次，金币、部署、生成、死亡顺序和胜负一致。
- [x] 自动运行至少 20 局完整比赛，不出现 NaN、负金币、超上限金币、重复格子或无法结束的战斗；当前竞技场为 32 局。
- [x] 测试从统一规则配置读取数值，重点验证费用范围、100 的倍数、周期为正数和数量上限等约束，避免在多个测试中复制同一个平衡数值。

平衡调优：

- [x] 移除双方全部初始兵种，比赛只保留城堡、初始金币和后续部署。
- [x] 剑士、弓箭手、法师和投石车分别按 3、2、2、1 的固定编制购买。
- [x] 新增独立 `pnpm arena` 命令，在不启动主程序的情况下运行 32 场左右桥换边对局。
- [x] 竞技场输出配置侧每百金币生命/秒伤，以及实战伤害、城堡伤害、胜平负和存活率。
- [x] 竞技场按被动金币每 2.8 秒恢复 100 的规则显示额外储蓄时间；相对 300 金币弓箭手，剑士、法师、投石车分别额外等待 2.8、8.4、14 秒。
- [x] 当前实战每百金币总伤害随费用整体小幅递增，并强制最贵与最便宜兵团的效率差不超过 20%；收束中央战场后的当前基线为 65.921、74.364、75.235、78.998，最大差距约 19.8%。
- [ ] 记录每局金币获得、金币溢出、部署选择、金矿回本时间、兵营实际出兵数和双方城堡剩余生命。
- [ ] 记录各兵种的存活时间、造成伤害、承受伤害、击杀数和抵达城堡比例。
- [ ] 用固定地图和固定随机种子批量模拟，比较不同配置下的胜率、平均比赛时长和五分钟同血平局率。
- [ ] 每轮只调整一组相关参数，并保留配置版本和对局结果，避免同时修改过多变量后无法判断影响来源。
- [ ] 首版验收以当前基线数值为准；后续版本允许调整全部经济、建筑、出兵和兵种属性，不把现有数值视为兼容性承诺。

浏览器验收：

- [ ] 验证 500 初始金币、1000 上限和第五分钟双倍恢复。
- [ ] 验证满额提示只在每次进入满额时播放一次。
- [ ] 分别部署六种项目，检查费用、预览、取消和无效位置。
- [ ] 填满所有建筑格，确认建筑按钮禁用；摧毁一座后对应格子恢复。
- [ ] 观察金矿完整寿命、提前被毁和满额浪费三种情况。
- [ ] 观察兵营完整生成周期和生成位置被占用的情况。
- [ ] 观察近战、远程和投石车，确认没有任何主动后撤。
- [ ] 让士兵在攻击城堡后受到围攻，确认目标始终保持为城堡。
- [ ] 观察城堡从休眠到激活，再到被摧毁和胜负结算。
- [ ] 让双方城堡以不同生命值存活到五分钟结束，确认生命较高方获胜；同血时判为平局。
- [ ] 浏览器控制台没有 warning 或 error。

## 实施顺序和质量规则

阶段 0 到阶段 7 按依赖顺序推进，阶段 8 的模型和音效准备可以在阶段 3 以后并行。每个阶段先补会失败的规则测试，再实现最小行为，最后整理模块并运行相关测试。阶段结束运行 `pnpm check`，不能把失败留给后续阶段处理。

模拟层继续使用固定步长。金币、建筑生产、自然掉血、攻击和城堡锁定都按模拟时间更新，暂停游戏时全部暂停，不能混用 `setTimeout` 或浏览器墙上时间。所有需要稳定排序的地方使用格子坐标、创建序号或实体 ID，不能依赖对象遍历顺序和无种子随机数。

UI 只提交部署意图和显示失败原因，场景只表现状态与事件。费用是否足够、格子能否使用、建筑是否到期、士兵该打谁和比赛是否结束，都由纯 TypeScript 模拟层决定。

平衡参数不得以魔法数字散落在组件、AI 或战斗循环中。配置调整后，部署栏、提示文案、AI 决策和模拟结果必须同步使用新值；测试优先验证规则不变量和配置一致性，只有本轮验收用例才固定当前基线数值。

## 已知风险

### 全部项目始终可选，容易形成建筑堆叠

皇室战争用卡牌循环限制同一张牌的连续部署，本项目暂时不做卡组。每个阵营同时最多存在 1 座金矿和 2 座兵营，这个限制由部署层统一执行，不能只禁用玩家按钮。建筑死亡并移除后，对应名额恢复。

### 建筑占满六边形后可能堵死路线

地图数据保留道路和桥头通道用于地表表现与导航引导，但己方领地内的空地允许建造。建筑封路由战斗系统处理：进攻单位优先攻击路线上的敌方建筑。

### 原有战斗系统包含手动命令和后撤逻辑

`src/game/battle.ts` 当前同时处理命令、射程保持、败退和自动目标。阶段 5 需要删除旧行为及其测试入口，同时保留投射物、伤害、攻击槽位和确定性基础，不能在旧后撤逻辑上继续叠条件分支。

### 金币和生产周期跨越倍率边界

2.8 秒、1.4 秒、金矿周期和固定模拟步长不会总在同一帧对齐。所有周期使用累计进度并明确同一时刻的处理顺序，大步长和小步长测试必须得到相同结果。

### 城堡锁定会改变普通索敌假设

城堡锁定是单位的永久状态，不是普通目标优先级。第一次城堡攻击开始后直接绕过普通候选目标查询，避免后续增加嘲讽、受击反应或建筑优先级时破坏这条规则。
