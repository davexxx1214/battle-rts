# 阶段 9：灰盒试玩、平衡与性能报告

记录日期：2026-08-21
分支：`codex/sandbox-mode`

## 结论

阶段 9 的自动化平衡与性能硬化已完成。当前沙盒大地图通过逻辑步、导航、内存、小地图和真实浏览器 100/200 实体预算；未调整已经锁定的建筑、兵种、矿量或人口维护数值。阶段尚不签核，原因是 3 场真人 30 分钟完整试玩与阶段 0 的旧 80 单位浏览器基线当时没有留下可比较记录。

## 三场 30 分钟确定性耐久场

这三场是受控自动化耐久场：城堡与中路守军在 30 分钟采样窗内不会死亡，采样结束后注入城堡摧毁状态以验证结算。它们用于验证经济、科技、AI 指挥、占矿、长局队列上界与结算，不等同于真人完整试玩。

| 场景 | 首矿 | 首基础兵 | 首高级建筑 | 首高级兵 | 首次接敌 | 首次中立矿争夺 | 结算 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 人类镜像 / 中路 | 1s | 15s | 33s | 80s | 1s | 133s | 1800.1s |
| 亡灵对人类 / 西路 | 1s | 15s | 55s | 205s | 1s | 28s | 1800.1s |
| 人类对亡灵 / 东路 | 1s | 15s | 35s | 87s | 1s | 154s | 1800.1s |

三场均以 `castle-destroyed` 正确结算；事件窗口最大值分别为 42、24、36。耐久场在路线中心预置高生命守军，因此 1 秒首次接敌只验证持续交战链路，不代表真人对局节奏。

## 平衡决策

- 保留人口上限 100，以及 0–50 为 100%、51–80 为 80%、81–100 为 60% 的矿收档位。沙盒每笔订单只生成一个单位，法师和重型单位分别占 2/4 人口；跨档仍严格按实际已用人口在下一次采矿结算生效。
- 两座安全矿总矿量 6000，加初始 1000 金币，足以承担两座矿、一座兵营和最低成本的 50 人口部队。两座中立矿在 80% 档提供 4800 有效金币，覆盖 50→80 的最低扩军成本。一座矿在 60% 档只有 1800 有效金币，不足以独自覆盖 80→100 的最低 2000 金币，后期需要更宽的地图控制。
- 保留中路 30 步、两条侧路 38 步。侧路路程约多 26.7%，但四个中立矿到侧路中心线不超过 3 格，形成明确的速度/资源交换。
- 保留所有沙盒建筑首版成本、工期和生命值。现有时序已在 1/18/47–70/87–162 秒形成清楚的资源、基础兵和高级兵阶段，没有发现需要通过暗改数值解决的软锁。
- 保留 AI 进攻集结门槛为至少 3 个单位、10 人口；单兵经济下三场首个高级单位为 80–205 秒，中立矿争夺为 28–154 秒，分别落在 240/180 秒耐久验收上限内。
- 保留守卫塔 500 金币、8 秒、450 生命、7 伤害、7 射程、1.35 秒冷却，每方最多 2 座。建造锚点都在城堡 6 格核心区内且避开道路；即使把全部 68 个锚点同时视作阻挡，三路仍连通。

## 模拟、内存与导航

压力场包含完整沙盒 AI 决策，不只调用基础战斗步。

| 场景 | 模拟步 p95 | 最大模拟步 | 每分钟堆增长 | 最大事件窗口 | 小地图 p95 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 100 实体 / 每方 50 人口 | 1.467ms | 9.908ms | 0.037MB | 366 | 0.266ms |
| 200 实体 / 每方 100 人口 | 3.331ms | 10.879ms | 0MB | 764 | 0.178ms |

1000 次确定性冷寻路全部成功：旧地图 p95 0.035ms / max 0.082ms，大地图 p95 0.170ms / max 0.347ms。动态生产出口、阻挡释放、事件窗口、矿业日志、AI 日志、音频池和战斗特效对象池继续由现有回归测试覆盖。

## 浏览器渲染

新增三个可复测入口：

- `?benchmark=80`
- `?benchmark=sandbox-100`
- `?benchmark=sandbox-200`

基准先预热 2 秒，再采样 10 秒，并只在采样结束时更新 React 面板，避免测量界面本身制造尖峰。沙盒在全图概览且达到 100 实体时使用按兵种实例化的战术 LOD；放大超过 zoom 40 后恢复完整单位模型。

| 场景 | 平均 FPS | 中位 FPS | 1% low | Draw calls | Triangles |
| --- | ---: | ---: | ---: | ---: | ---: |
| 沙盒 100 实体 | 60.0 | 59.9 | 49.9 | 69 | 81,143 |
| 沙盒 200 实体 | 60.0 | 59.9 | 44.1 | 69 | 85,007 |

优化前，200 实体为平均 33.2 FPS、1% low 19.4、1919 draw calls、1,487,087 triangles。优化后满足阶段预算；69 个 draw calls 也证明 871 个地块没有形成近似逐格绘制提交。

当前旧 80 单位场在 10 秒内记录 275 帧（约 27.5 FPS）、中位 28.4 FPS、1% low 13.7、2350 draw calls。阶段 0 没有记录当时的浏览器数字，因此无法诚实计算“回退不超过 20%”；战术 LOD 明确只对沙盒生效，没有改变旧模式渲染路径。

## 回归与待办

- `pnpm check`：107 个测试文件、695 项测试通过；lint、typecheck、音频、模型、场景资产和生产构建全绿。
- `pnpm arena`：3 个测试文件、5 项场景通过。原有的两条错误平衡假设已改为角色化门槛：法师保持最高对单位效率、投石车保持最高对城效率、兵种总效率差不超过 1.5 倍；跨种族换边要求无地图方位偏置、单位伤害差不超过 25%、残兵差不超过 2。
- 待办：由真人完成至少 3 场、每场至少 30 分钟的完整灰盒对局，记录同一组里程碑以及 50/80 人口扩军体感。
- 待办：如必须签核旧 80 回退比例，需要从阶段 0 对应提交建立独立可运行基准并补录，或由项目负责人接受“未记录基线”例外。
